<?xml version='1.0' encoding='utf-8'?>
<effect category_ultra="Glow & Gradients" id="com.alightcreative.effects.extra3d.synthwave-floor" name="Retro Grid 3D" desc="Projects an infinite scrolling neon grid floor into 3D perspective for a retrowave vibe." category="color" thumb="thumb/5/hardg.png" tags="3d,grid,retro,synthwave,floor,raster,animated">

    <params>
        <texture id="inputImg" srcType="content" />
        <color id="color" default="#FFFF2BD6" label="Grid Color" alpha="false" />
        <slider id="horizon" default="0.5" min="0.2" max="0.8" label="Horizon" />
        <slider id="speed" default="1.0" min="0.0" max="4.0" label="Speed" />
        <slider id="amount" default="0.9" min="0.0" max="1.0" label="Opacity" type="percent" />
    </params>

    <shader type="fragment" precision="high">
        void main(){
            vec4 src=texture2DCv(inputImg.texture, acScreenNorm);
            vec3 c=src.rgb;
            vec2 uv=acScreenNorm;
            float grid=0.0;
            if(uv.y &lt; horizon){
                float z=1.0/max(horizon-uv.y, 0.001);
                float wx=(uv.x-0.5)*z;
                float wz=z + acTime*speed;
                float gx=abs(fract(wx)-0.5);
                float gz=abs(fract(wz)-0.5);
                float lw=0.03*z;
                grid=1.0-smoothstep(0.0, lw, min(gx,gz));
                grid*=clamp(z*0.15,0.0,1.0);
            }
            vec3 outc=1.0-(1.0-c)*(1.0-color.rgb*grid*amount);
            gl_FragColor=vec4(outc, src.a) + vec4(color.rgb*grid*amount,0.0)*(1.0-src.a);
        }
    </shader>

</effect>