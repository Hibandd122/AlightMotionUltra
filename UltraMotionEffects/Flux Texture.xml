<?xml version='1.0' encoding='utf-8'?>
<effect category_ultra="Glow & Gradients" id="com.alightcreative.effects.textureflux" name="Flux Texture" desc="Generates a flowing abstract texture by iteratively warping coordinates with cosine feedback, then coloring with sine palettes. Controls include iteration amount, warp/flow vectors, RGB factors, gamma, optional two-color gradient map, transform and blend modes." category="color" tags="procedural" thumb="thumb/11/4.png" experimental="false">

    <params>
        <switch id="autop" default="true" label="Auto Progress" />
        <slider id="AMOUNT" label="Amount" default="40" min="0" max="50" step="1.0" type="integer" snap="5,10,15,20,25,30,35,40,45" />
        <slider id="PROGRESS" label="Progress" default="1.0" min="-100.0" max="100.0" step="0.01" type="percent" />
        <slider id="SPEED" label="Speed AutoProg" default="1.0" min="0.0" max="100.0" step="0.01" type="percent" />
        <point id="factor" default="0.300, 0.400" min="0.001, 0.001" max="2, 2" label="Control Warp" step="0.001" />
        <point id="freq" default="1.3, 1.7" min="0.01, 0.01" max="5, 5" label="Control Flow" step="0.001" />
        <slider id="scale" default="0.5" min="0.1" max="50.0" step="0.01" label="Scale" type="percent" />
        <slider id="angle" default="0.0" min="-3600" max="3600" ticks="-180,-135,-90,-45,-30,-15,0,15,30,45,90,135,180" label="Rotate" step="0.5" type="angle" />
        <slider id="col1" label="Red Factor" default="0.6" min="0.0" max="2.0" step="0.01" type="percent" />
        <slider id="col2" label="Green Factor" default="0.75" min="0.0" max="2.0" step="0.01" type="percent" />
        <slider id="col3" label="Blue Factor" default="0.2" min="0.0" max="2.0" step="0.01" type="percent" />
        <slider id="gamma" label="Gamma" default="1.0" min="0.0" max="2.0" step="0.01" type="percent" />
        <switch id="GRADMAP" default="false" label="Gradint Map" />
        <color id="color1" default="#FF000000" label="Color BG" alpha="true" />
        <color id="color2" default="#FFFF5500" label="Color FG" alpha="true" />
        <point id="position" label="@am:string/effect_param_offset" />
        <point id="stretch" default="1.0,1.0" min="0.01,0.01" max="10, 10" label="@am:string/effect_param_stretch" step="0.01" />
        <slider id="alpha" default="1.0" min="0.0" max="1.0" step="0.01" label="Alpha" type="percent" />
        <slider id="hardness" default="1.0" min="1.0" max="3.0" step="0.01" label="Hard/Brightness" type="percent" />
        <menu id="blendMode" style="radio" default="3">
            <menu label="NORMAL" value="0" />
            <menu label="LAYER" value="3" />
            <menu label="MULTIPLY" value="1" />
            <menu label="SCREEN" value="2" />
        </menu>
    <texture id="inputImg" srcType="content" />
    <texture id="xcipher" srcType="buffer" downsample="2" />
    </params>
    
    <passes>
        <pass target="xcipher" />
        <pass />
    </passes>
    
    

    <shader type="fragment">
    
    vec4 newLayer( vec4 a, vec4 b) { return a * (1. - b.a) + b;  }
    mat2 rot2m(float a){ float s = sin(a); float c = cos(a); return mat2(c, -s, s, c); }
    vec2 l2s( vec2 v ) { return (vec4(v,0.,1.)*acLayerToScreen).xy; }
    #define fragCoord gl_FragCoord.xy
    
    //////////////////////////
        
    //////////////////////////
        void main() {
        if( acPass == 0 ) { gl_FragColor = texture2DCv(inputImg.texture, acScreenNorm.st); } else if( acPass==1 ) {
        vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
        vec4 texColorX = vec4(0.0,0.0,0.0,1.0) * texColor.a;
        
        vec2 image_size = getTexSize(inputImg.size);
        vec2 iResolution = vec2(acScreenSize.x, acScreenSize.y);
        vec2 iMouse = vec2(0.5, 0.5);
        float iTime = autop? PROGRESS + (acTime*SPEED) : PROGRESS;
        
        vec2 umefx;
        // umefx = acScreenNorm; 
        umefx = ((vec2(acScreenNorm.x,1.0-acScreenNorm.y)*acScreenSize)-acLayerCenter)/acLayerSize;
        umefx.y*=acLayerSize.y/acLayerSize.x;
        
        umefx /= scale;
        umefx *= rot2m((0.0+angle)*0.0174533);
        umefx += vec2((0.0+position.x), (0.0+position.y))*vec2(-1.,1.)/500.;
        vec2 scaleVec;
        scaleVec = vec2(scale*stretch.x,scale*stretch.y);
        umefx /= scaleVec;
        
        /////////////////////////
        
    	vec4 fragColor;
    	
            vec2 q= umefx;
            // 7.0*(fragCoord.xy-0.5*iResolution.xy)/max(iResolution.x,iResolution.y);
        	
        	for(float i=1.0; i&lt; AMOUNT; i*=1.1)
        	{
        		vec2 o=q;
        		o.x+=(0.5/i)*cos(i*q.y+iTime*factor.x+0.03*i)+1.3;		
        		o.y+=(0.5/i)*cos(i*q.x+iTime*factor.y+0.03*(i+10.0))+1.9;
        		// o.x+=(0.5/i)*cos(i*q.y+iTime*0.297+0.03*i)+1.3;		
        		// o.y+=(0.5/i)*cos(i*q.x+iTime*0.414+0.03*(i+10.0))+1.9;
        		q=o;
        	}
        
        	// vec3 col=vec3(0.5*sin(strength.x*q.x)+0.5,
        	// 0.5*sin(strength.y*q.y)+0.5,
        	vec3 col=vec3(0.5*sin(3.0*q.x)+0.5,
        	0.5*sin(3.0*q.y)+0.5,
        	
        	sin(freq.x*q.x+freq.y*q.y));
        	// sin(1.3*q.x+1.7*q.y));
        	
                float f= (gamma-0.57)*(col.x+col.y+col.z);
                // float f=0.43*(col.x+col.y+col.z);
        
        	fragColor= 
        	vec4(f+col1,
        	0.2+col2*f, 
        	col3, 1.0);
        	// vec4(f+0.6,
        	// 0.2+0.75*f, 
        	// 0.2,1.0);
        
    	    const vec3 luminanceWeighting = vec3(0.2126, 0.7152, 0.0722);
            float luminance = dot(fragColor.rgb, luminanceWeighting);
            vec4 gradmap = mix(color1,color2,luminance)*fragColor.a;
            
    	    // vec4 factor = mix(texColorX,gradmap,1.0);
    	    // if(GRADMAP) { factor = mix(texColorX,fragColor,1.0); }
    	    vec4 factor = mix(texColorX,fragColor,1.0);
    	    if(GRADMAP) { factor = mix(texColorX,gradmap,1.0); }

        vec4 resultfinal;
          if( blendMode==0 ) { resultfinal = (factor * hardness) * texColor.a; }
          else if( blendMode==1 ) { factor *= hardness;
            vec4 c = factor + vec4(1.)*(1.-factor.a);
            resultfinal = vec4((texColor.rgb/texColor.a)*c.rgb,1.0) * texColor.a; }
            // resultfinal = min(factor, texColor) * texColor.a; }
          else if( blendMode==2 ) { factor *= hardness; 
            resultfinal = vec4( 1.-((1.-(texColor.rgb/texColor.a))*(1.-factor.rgb)),1.0) * texColor.a; }
              // resultfinal = max(factor, texColor) * texColor.a; }
          else if( blendMode==3 ) { factor *= hardness; resultfinal = (newLayer(texColor, factor) * hardness) * texColor.a; }
            
    	gl_FragColor = mix(texColor, resultfinal, alpha) * texColor.a;
          }
        }

    </shader>

</effect>