<?xml version='1.0' encoding='utf-8'?>
<effect category_ultra="Flares, Rays & Energy" id="com.alightcreative.effects.extra2.smear" name="Smear" desc="Directional motion smear that streaks the layer along a chosen angle." category="color" thumb="thumb/5/hardg.png" tags="distort,blur,motion,smear,raster" overdraw="true" max-overdraw="1.0">

    <params>
        <texture id="inputImg" srcType="content" />
        <slider id="angle" default="0.0" min="0.0" max="360.0" label="Direction" type="angle" step="1" />
        <slider id="size" default="0.05" min="0.0" max="0.3" label="Length" />
    </params>

    <shader type="fragment" precision="high">
        void main(){
            float a = radians(angle);
            vec2 dir = vec2(cos(a), sin(a))*size;
            vec4 acc = vec4(0.0);
            for(int i=0;i&lt;8;i++){
                acc += texture2DCv(inputImg.texture, acScreenNorm - dir*(float(i)/8.0));
            }
            gl_FragColor = acc/8.0;
        }
    </shader>

</effect>