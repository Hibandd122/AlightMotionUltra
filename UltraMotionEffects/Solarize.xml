<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.effects.extra2.solarize" name="Solarize" desc="Inverts only the tones above a threshold, producing a surreal solarized exposure." category="colorlight" thumb="thumb/Color/Solarize.webp" tags="color,solarize,surreal,raster">

    <params>
        <texture id="inputImg" srcType="content" />
        <slider id="threshold" default="0.5" min="0.0" max="1.0" label="Threshold" />
        <slider id="amount" default="1.0" min="0.0" max="1.0" label="Amount" type="percent" />
    </params>

    <shader type="fragment" precision="high">
        void main(){
            vec4 src = texture2DCv(inputImg.texture, acScreenNorm);
            vec3 c = src.a&gt;0.0? src.rgb/src.a : src.rgb;
            vec3 s = mix(c, 1.0-c, step(vec3(threshold), c));
            gl_FragColor = vec4(mix(c,s,amount),1.0)*src.a;
        }
    </shader>

</effect>