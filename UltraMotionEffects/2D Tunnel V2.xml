<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.effects.2Dtunnelv2" name="2D Tunnel V2" desc="Procedural tunnel of glowing grid lines built from a polar coordinate remap with an adjustable Minkowski distance metric. Controls include grid amount, line thickness, rotation, sinus pattern, progress/speed, and a two-color gradient map with blend modes." category="procedural" tags="other" thumb="thumb/9/tunnelv2.png" experimental="false">

    <params>
        <color id="color1" default="#FF000000" label="Color BG" alpha="true" />
        <color id="color2" default="#FF00FFFF" label="Color FG" alpha="true" />
        <switch id="autop" default="true" label="Auto Progress" />
        <switch id="newpath" default="false" label="Sinus Pattern" />
        <slider id="PROGRESS" label="Progress" default="1.0" min="-100.0" max="100.0" step="0.01" type="percent" />
        <slider id="SPEED" label="Speed" default="0.4" min="0.0" max="100.0" step="0.01" type="percent" />
        <point id="position" label="Position" />
        <slider id="xxj" label="Sharpen" default="2.5" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="xxg" label="Thickness V1" default="0.12" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="xxi" label="Thicknes V2" default="0.12" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="xxe" label="Amount Grid" default="5" min="0.0" max="50.0" step="0.5" />
        <slider id="xxc" label="Circular Grid" default="0.25" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="xxd" label="Rotate Grid" default="1.2" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="xxb" label="Grid Z" default="0" min="0.0" max="100.0" step="0.01" type="percent" />
        <point id="xxx" default="4.0, 4.0" min="-10., -10." max="10, 10" label="Factor" step="0.01" />

        <slider id="scale" default="1.0" min="0.1" max="50.0" step="0.01" label="Scale" type="percent" />
        <slider id="angle" default="0.0" min="-3600" max="3600" ticks="-180,-135,-90,-45,-30,-15,0,15,30,45,90,135,180" label="Rotate" step="0.5" type="angle" />
        <point id="stretch" default="1.0,1.0" min="0.01,0.01" max="10, 10" label="@am:string/effect_param_stretch" step="0.01" />
        <slider id="alpha" default="1.0" min="0.0" max="1.0" step="0.01" label="Alpha" type="percent" />
        <slider id="hardness" default="1.0" min="1.0" max="3.0" step="0.01" label="Hard/Brightness" type="percent" />
        <menu id="blendMode" style="radio" default="3">
            <menu label="NORMAL" value="0" />
            <menu label="LAYER" value="3" />
            <menu label="MULTIPLY" value="1" />
            <menu label="SCREEN" value="2" />
        </menu>
    <texture id="inputImg" srcType="content" />
    <texture id="xcipher" srcType="buffer" downsample="2" />
    </params>
    
    <passes>
        <pass target="xcipher" />
        <pass />
    </passes>
    
    

    <shader type="fragment">
    //////////////////////////
    vec4 newLayer( vec4 a, vec4 b) { return a * (1. - b.a) + b;  }
    mat2 rot2m(float a){ float s = sin(a); float c = cos(a); return mat2(c, -s, s, c); }
    vec2 l2s( vec2 v ) { return (vec4(v,0.,1.)*acLayerToScreen).xy; }
    #define fragCoord gl_FragCoord.xy
    
   /////////////////////////
   
        #define PI 3.141592653589793
        #define TAU 6.283185307179586
        
        float cubicPulse( float c, float w, float x ){
            x = abs(x - c);
            if( x&gt;w ) return 0.0;
            x /= w;
            return 1.0 - x*x*(3.0-2.0*x);
        }
    //////////////////////////
        void main() {
        if( acPass == 0 ) { gl_FragColor = texture2DCv(inputImg.texture, acScreenNorm.st); } else if( acPass==1 ) {
        vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
        vec4 texColorX = vec4(0.0,0.0,0.0,1.0) * texColor.a;
        
        vec2 image_size = getTexSize(inputImg.size);
        vec2 iResolution = vec2(acScreenSize.x, acScreenSize.y);
        vec2 iMouse = vec2(0.5, 0.5);
        float iTime = autop? PROGRESS + (acTime*SPEED) : PROGRESS;
        
        vec2 umefx;
        umefx = ((vec2(acScreenNorm.x,1.0-acScreenNorm.y)*acScreenSize)-acLayerCenter)/acLayerSize;
        umefx.y*=acLayerSize.y/acLayerSize.x;
        
        umefx /= scale;
        umefx *= rot2m((0.0+angle)*0.0174533);
        umefx += vec2((0.0+position.x), (0.0+position.y))*vec2(-1.,1.)/500.;
        vec2 scaleVec;
        scaleVec = vec2(scale*stretch.x,scale*stretch.y);
        umefx /= scaleVec;
        
        /////////////////////////
        
    	vec4 fragColor;
    	
            float time = iTime;
            vec2 p = umefx;
            vec2 uvOrig = p;
            float rotZ = 1.0 - 0.23 * sin(1.0 * cos(length(p * 1.5)));
            p *= mat2(cos(rotZ), sin(rotZ), -sin(rotZ), cos(rotZ));

            float a = atan(p.y,p.x);												// angle of each pixel to the center of the screen
            float rSquare = pow( pow(p.x*p.x,xxx.x) + pow(p.y*p.y,xxx.y), 1.0/8.0 );	// modified distance metric (http://en.wikipedia.org/wiki/Minkowski_distance)
            float rRound = length(p);
            float r = mix(rSquare, rRound, 0.5 + 0.5 * sin(time * 2.0)); 			// interp between round &amp; rect tunnels
            vec2 uv = vec2( 0.3/r + time, a/3.1415927 );							// index texture by (animated inverse) radious and angle
        
            // subdivide to grid
            float PAT = newpath? sin(time + uv.x * xxd) : time + (uv.x * xxd);
            uv += vec2(xxb, xxc * PAT);			// pre-warp
            uv /= vec2(1.0 + 0.0002 * length(uvOrig));
            vec2 uvDraw = fract(uv * xxe);							// create grid
        
            float col = cubicPulse(0.5, xxg, uvDraw.x);
            col = max(col, cubicPulse(0.5, xxi, uvDraw.y));
                
            col = col * r * xxj;
            col += 0.0 * length(uvOrig);
            
            fragColor = vec4(vec3(0.0, col, 0.0), 1.0);
            
    	    const vec3 luminanceWeighting = vec3(0.2126, 0.7152, 0.0722);
            float luminance = dot(fragColor.rgb, luminanceWeighting);
            vec4 gradmap = mix(color1,color2,luminance)*fragColor.a;
    	    vec4 factor = mix(texColorX,gradmap,1.0); 
    	    
        vec4 resultfinal;
          if( blendMode==0 ) { resultfinal = (factor * hardness) * texColor.a; }
          else if( blendMode==1 ) { factor *= hardness;
            vec4 c = factor + vec4(1.)*(1.-factor.a);
            resultfinal = vec4((texColor.rgb/texColor.a)*c.rgb,1.0) * texColor.a; }
            // resultfinal = min(factor, texColor) * texColor.a; }
          else if( blendMode==2 ) { factor *= hardness; 
            resultfinal = vec4( 1.-((1.-(texColor.rgb/texColor.a))*(1.-factor.rgb)),1.0) * texColor.a; }
              // resultfinal = max(factor, texColor) * texColor.a; }
          else if( blendMode==3 ) { resultfinal = (newLayer(texColor, factor) * hardness) * texColor.a; }
            
    	gl_FragColor = mix(texColor, resultfinal, alpha) * texColor.a;
          }
        }

    </shader>

</effect>