<?xml version='1.0' encoding='utf-8'?>
<effect category_ultra="Nature & Weather" id="com.alightcreative.effects.newlensflare" name="New Lensflare" desc="Procedural lens flare: layers ten pseudo-random circles, rings and hexagonal ghosts along the flare axis plus a sun core with animated rays over a two-color sky gradient. Orient X/Y aim the flare; Strength, Reflection and Luminance scale its components." category="procedural" tags="other" thumb="thumb/2/newflare.png" experimental="false">

    <params>
        <texture id="inputImg" srcType="content" />
        
        <point id="position" label="Position" default="0.0,0.0" />
        <slider id="progressc" label="Orient X" default="1.0" min="-10.0" max="10.0" step="0.01" type="percent" />
        <slider id="progressd" label="Orient Y" default="0.0" min="0.0" max="10.0" step="0.025" type="percent" />
        
        <slider id="progresse" label="Strength" default="0.25" min="0.1" max="10.0" step="0.01" type="percent" />
        <slider id="progressa" label="Reflection" default="1.8" min="0" max="1000.0" step="0.01" type="percent" />
        <slider id="progressb" label="Luminance" default="1.0" min="0" max="10.0" step="0.01" type="percent" />
        
        
        <slider id="alpha" default="1.0" min="0.0" max="1.0" step="0.01" label="Alpha" type="percent" />
        <color id="color1" default="#FF703D33" label="Flare Color" alpha="true" />
        
        <color id="color3" default="#FF4C3305" label="Base Color" alpha="true" />
        <color id="color4" default="#FF3380CC" label="Secondary Color" alpha="true" />
        <menu id="blendMode" style="radio" default="0" label="@am:string/effect_param_blendmode">
            <menu label="NORMAL" value="0" />
            <menu label="MULTIPLY" value="1" />
            <menu label="SCREEN" value="2" />
        </menu>

    </params>
    
    <shader type="fragment" precision="high">
    
    mat2 rot2m(float a){
        
	float s = sin(a);
	float c = cos(a);
	
	return mat2(c, -s, s, c);
    }
    
    vec2 l2s( vec2 v ) {
        return (vec4(v,0.,1.)*acLayerToScreen).xy;
    }
    
   #define fragCoord gl_FragCoord
    
   /////////////////////////
   
        float rnd(vec2 p)
      {
          float f = fract(sin(dot(p, vec2(12.1234, 72.8392) )*45123.2));
       return f;   
      }
      
      float rnd(float w)
      {
          float f = fract(sin(w)*1000.);
       return f;   
      }
      
      float regShape(vec2 p, int N)
      {
       float f;
          
          
      float a=atan(p.x,p.y)+.2;
      float b=6.28319/float(N);
      f=smoothstep(.5,.51, cos(floor(.5+a/b)*b-a)*length(p.xy));
          
          
          return f;
      }
      vec3 circle(vec2 p, float size, float decay, vec3 color,vec3 color2, float dist, vec2 mouse)
      {
       	 
          
          //l is used for making rings.I get the length and pass it through a sinwave
          //but I also use a pow function. pow function + sin function , from 0 and up, = a pulse, at least
          //if you return the max of that and 0.0.
          
          float l = length(p + mouse*(dist*4.))+size/2.;
          
          //l2 is used in the rings as well...somehow...
          float l2 = length(p + mouse*(dist*4.))+size/3.;
          
          ///these are circles, big, rings, and  tiny respectively
          float c = max(00.01-pow(length(p + mouse*dist), size*1.4), 0.0)*50.;
          float c1 = max(0.001-pow(l-0.3, 1./40.)+sin(l*30.), 0.0)*3.;
          float c2 =  max(0.04/pow(length(p-mouse*dist/2. + 0.09)*1., 1.), 0.0)/20.;
          float s = max(00.01-pow(regShape(p*5. + mouse*dist*5. + 0.9, 6) , 1.), 0.0)*5.;
          
         	color = 0.5+0.5*sin(color);
          color = cos(vec3(color1.rgb)*8. + dist*4.)*0.5+.5;
          // color = cos(vec3(0.44, .24, .2)*8. + dist*4.)*0.5+.5;
       	vec3 f = c*color ;
          f += c1*color;
          
          f += c2*color;  
          f +=  s*color;
          return f-0.01;
      }
      
      float sun(vec2 p, vec2 mouse)
      {
       float f;
          
          vec2 sunp = p+mouse;
          float sun = 1.0-length(sunp)*0.0;
          return f;
      }

   
   /////////////////////////
    void main ()
    {
            
        vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
        vec4 texColorX = vec4(0.0,0.0,0.0,1.0) * texColor.a;
        
        vec2 umefx;
        // if (adaptive) { 
        umefx = acScreenNorm; 
        // }
        // else { 
        // umefx = ((vec2(acScreenNorm.x,1.0-acScreenNorm.y)*acScreenSize)-acLayerCenter)/acLayerSize;
        // umefx.y*=acLayerSize.y/acLayerSize.x;
        // }

        umefx /= (position.z/100.0);
        // umefx *= rot2m((0.0+angle)*0.0174533);
        umefx += vec2((230.0+position.x), (-130.0+position.y))*vec2(-1.,1.)/500.;

        
        /////////////////////////
        
        // if(progressa &lt;= 0.0) { gl_FragColor = texture2DCv(inputImg.texture,acScreenNorm); }
        // else if(progressa &gt;= 0.0001) { 

    	vec4 fragColor;
    	
          vec2 uv = umefx;
          //uv=uv*2.-1.0;
          uv.x*=acScreenSize.x/acScreenSize.y;
          
          vec2 mm = uv;
          // vec2 mm = mousee/umefx - 0.5;
          // mm.x *= mousee/umefx.y;
          
          // if(progressa&lt;1.0)
          // {
           mm = vec2(sin(progressc/6.)/1., cos(progressd/4.0)/2. )/2.;   
          // }
          vec3 circColor = vec3(0.9, 0.2, 0.1);
          vec3 circColor2 = vec3(0.3, 0.1, 0.9); //05
          
          //now to make the sky not black
          vec3 color = mix(vec3(color3.rgb)/0.9, vec3(color4.rgb), uv.y)*progressb;
          // vec3 color = mix(vec3(0.3, 0.2, 0.02)/0.9, vec3(0.2, 0.5, 0.8), uv.y)*progressb;
          // vec3 color = mix(vec3(0.3, 0.2, 0.02)/0.9, vec3(0.2, 0.5, 0.8), uv.y)*3.-0.52*sin(progressb);
          
          //this calls the function which adds three circle types every time through the loop based on parameters I
          //got by trying things out. rnd i*2000. and rnd i*20 are just to help randomize things more
          for(float i=0.;i&lt;10.;i++){
              color += circle(uv, pow(rnd(i*2000.)*progressa, 2.)+1.41, 0.0, circColor+i , circColor2+i, rnd(i*20.)*3.+0.2-.5, mm);
          }
          //get angle and length of the sun (uv - mouse)
            float a = atan(uv.y-mm.y, uv.x-mm.x); //19
          	float l = max(1.0-length(uv-mm)-0.84, 0.0);

          float bright = progresse;//+0.1/abs(sin(iTime/3.))/3.;//add 1.0ness based on how the sun moves so that it is 1.0est
          //when it is lined up with the center
          
          //add the sun with the frill things
          color += max(0.1/pow(length(uv-mm)*5., 5.), 0.0)*abs(sin(a*5.+cos(a*9.)))/20.;
          color += max(0.1/pow(length(uv-mm)*10., 1./20.), .0)+abs(sin(a*3.+cos(a*9.)))/8.*(abs(sin(a*9.)))/1.;
          //add another sun in the middle (to make it 1.0er)  with the20color I want, and 1.0 as the numerator.
          color += (max(bright/pow(length(uv-mm)*4., 1./2.), 0.0)*4.)*vec3(0.2, 0.21, 0.3)*4.;
          // color += (max(bright/pow(length(uv-mm)*4., 1./2.), 0.0)*4.)*vec3(color2.rgb)*4.;
             // * (0.5+.5*sin(vec3(0.4, 0.2, 0.1) + vec3(a*2., 00., a*3.)+1.3));
          	
          //multiply by the exponetial e^x ? of 1.0-length which kind of masks the 1.0ness more so that
          //there is a sharper roll of of the light decay from the sun. 
            color*= exp(1.0-length(uv-mm))/5.;
      	fragColor = vec4(color,1.0);

        
         	// const vec3 luminanceWeighting = vec3(0.2126, 0.7152, 0.0722);
            // vec4 gradmap;
            // float luminance = dot(fragColor.rgb, luminanceWeighting);
            // gradmap = mix(color1,color2,luminance)*fragColor.a;
        
    	// vec4 factorx = mix(fragColor,gradmap,alphab);
    	vec4 factor = mix(texColorX,fragColor,alpha);

        vec4 resultfinal;
        if( blendMode==0 ) {                // normal
            resultfinal = (factor * 1.0) * texColor.a;
        } else if( blendMode==1 ) {         // multiply
            factor *= 1.0;
            vec4 c = factor + vec4(1.)*(1.-factor.a);
            resultfinal = vec4((texColor.rgb/texColor.a)*c.rgb,1.0) * texColor.a;
        } else if( blendMode==2 ) {         // screen
            factor *= 1.0;
            resultfinal = vec4( 1.-((1.-(texColor.rgb/texColor.a))*(1.-factor.rgb)),1.0) * texColor.a;
        }
            
    	gl_FragColor = resultfinal;
    	// }
    }


    </shader>

</effect>