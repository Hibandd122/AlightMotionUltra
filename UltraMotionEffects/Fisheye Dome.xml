<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.effects.extra3d.dome-3d" name="Fisheye Dome" desc="Bulges the image with a fisheye-style dome remap (sin-based radial distortion) inside its inscribed circle; pixels outside the circle are untouched. Field of View sets the curvature and Amount blends against the original." category="3d" thumb="thumb/Distort and Warp/Fisheye Dome.webp" tags="3d,dome,fisheye,bulge,raster" overdraw="true" max-overdraw="1.0">

    <params>
        <texture id="inputImg" srcType="content" />
        <slider id="fov" default="120.0" min="40.0" max="170.0" label="Field of View" type="angle" step="1" />
        <slider id="amount" default="1.0" min="0.0" max="1.0" label="Amount" type="percent" />
    </params>

    <shader type="fragment" precision="high">
        void main(){
            vec2 p=acScreenNorm*2.0-1.0; float asp=acScreenSize.x/acScreenSize.y; p.x*=asp;
            float r=length(p);
            if(r&gt;1.0){gl_FragColor=texture2DCv(inputImg.texture, acScreenNorm);return;}
            float maxT=radians(fov)*0.5;
            float theta=r*maxT;
            float rr=sin(theta)/sin(maxT);
            vec2 dir = r&gt;0.0001? p/r : vec2(0.0);
            vec2 np=dir*rr; np.x/=asp;
            vec2 uv=mix(acScreenNorm, np*0.5+0.5, amount);
            gl_FragColor=texture2DCv(inputImg.texture, clamp(uv,0.0,1.0));
        }
    </shader>

</effect>