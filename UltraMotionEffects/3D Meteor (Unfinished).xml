<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.effects.3dmeteor" name="3D Meteor (Unfinished)" desc="Renders radial meteor streaks: the view direction is split into random angular slices, each assigned a random distance, phase and hue, shaded as exponentially fading streaks flying toward the viewer while the field slowly rotates. Unfinished: several controls are raw shader constants." category="procedural" tags="other" thumb="thumb/Procedural/3D Meteor (Unfinished).webp" experimental="false">

    <params>
        
        <switch id="autop" default="true" label="Auto Progress" />
        <slider id="PROGRESS" label="Progress" default="1.0" min="-100.0" max="100.0" step="0.01" type="percent" />
        <slider id="SPEED" label="Speed AutoProg" default="1.0" min="0.0" max="100.0" step="0.01" type="percent" />
        
        <slider id="xxa" label="Curvature" default="0.2" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="xxb" label="Angle Spread" default="2.0" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="xxc" label="Slice Count" default="1000" min="0.0" max="1500.0" step="1" type="integer" />
        <slider id="xxd" label="Phase Seed" default="0" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="xxe" label="Distance Seed" default="1" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="xxf" label="Spread Distance" default="3" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="xxg" label="Hue Seed" default="2" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="xxh" label="Streak Speed" default="0.6" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="xxi" label="Repeat Interval" default="1" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="xxj" label="Streak Falloff" default="8" min="0.0" max="100.0" step="0.01" type="percent" />
        
        <slider id="scale" default="1.0" min="0.1" max="50.0" step="0.01" label="Scale" type="percent" />
        <slider id="angle" default="0.0" min="-3600" max="3600" ticks="-180,-135,-90,-45,-30,-15,0,15,30,45,90,135,180" label="Rotate" step="0.5" type="angle" />
        <point id="position" label="@am:string/effect_param_offset" />
        <point id="stretch" default="1.0,1.0" min="0.01,0.01" max="10, 10" label="@am:string/effect_param_stretch" step="0.01" />
        <slider id="alpha" default="1.0" min="0.0" max="1.0" step="0.01" label="Alpha" type="percent" />
        <slider id="hardness" default="1.0" min="1.0" max="3.0" step="0.01" label="Hard/Brightness" type="percent" />
        <menu id="blendMode" style="radio" default="3">
            <menu label="NORMAL" value="0" />
            <menu label="LAYER" value="3" />
            <menu label="MULTIPLY" value="1" />
            <menu label="SCREEN" value="2" />
        </menu>
        
        
        
        <texture id="inputImg" srcType="content" />
        <texture id="buff1" srcType="buffer" />

    </params>
    
    <passes><pass target="buff1" /><pass /></passes>
    
    <shader type="fragment" precision="high">
    
    vec4 newLayer( vec4 a, vec4 b) { return a * (1. - b.a) + b;  }
    mat2 rot2m(float a){ float s = sin(a); float c = cos(a); return mat2(c, -s, s, c); }
    vec2 l2s( vec2 v ) { return (vec4(v,0.,1.)*acLayerToScreen).xy; }
    #define fragCoord gl_FragCoord.xy
    
   /////////////////////////
   
         vec3 hsv(float h, float s, float v) {
        	vec4 K = vec4(1.0, 2.0 / 3.0, 1.0 / 3.0, 3.0);
        	vec3 p = abs(fract(vec3(h) + K.xyz) * 6.0 - K.www);
        	return v * mix(K.xxx, clamp(p - K.xxx, 0.0, 1.0), s);
        }
        
        float rand(vec2 co){
            return fract(sin(dot(co.xy ,vec2(12.9898,78.233))) * 43758.5453);
        }
   
   /////////////////////////
    void main ()
    {
            
        vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
        vec4 texColorX = vec4(0.0,0.0,0.0,1.0) * texColor.a;
        
        vec2 image_size = getTexSize(inputImg.size);
        vec2 iResolution = vec2(acScreenSize.x, acScreenSize.y);
        vec2 iMouse = vec2(0.5, 0.5);
        float iTime = autop? PROGRESS + (acTime*SPEED) : PROGRESS;
        
        vec2 umefx;
        // umefx = acScreenNorm; 
        umefx = ((vec2(acScreenNorm.x,1.0-acScreenNorm.y)*acScreenSize)-acLayerCenter)/acLayerSize;
        umefx.y*=acLayerSize.y/acLayerSize.x;
        
        umefx /= scale;
        umefx *= rot2m((0.0+angle)*0.0174533);
        umefx += vec2((0.0+position.x), (0.0+position.y))*vec2(-1.,1.)/500.;
        vec2 scaleVec;
        scaleVec = vec2(scale*stretch.x,scale*stretch.y);
        umefx /= scaleVec;
        
        /////////////////////////
        
    	vec4 fragColor;
    	
        	vec2 p = umefx;
        	 // (2.0 * fragCoord.xy - iResolution.xy) / min(iResolution.x, iResolution.y);
        	vec3 v = vec3(p, 1.0 - length(p) * xxa);
        	// vec3 v = vec3(p, 1.0 - length(p) * 0.2);
        
        	float ta = iTime * 0.1;
        	mat3 m=mat3(
        		0.0,1.0,0.0,
        		-sin(ta),0.0,cos(ta),
        		cos(ta),0.0,sin(ta));
        	m*=m*m;
        	m*=m;
        	v=m*v;
        
        	float a = (atan(v.y, v.x) / 3.141592 / xxb + 0.5);
        	float slice = floor(a * xxc);
        	float phase = rand(vec2(slice, xxd));
        	float dist = rand(vec2(slice, xxe)) * xxf;
        	float hue = rand(vec2(slice, xxg));
        	// float a = (atan(v.y, v.x) / 3.141592 / 2.0 + 0.5);
        	// float slice = floor(a * 1000.0);
        	// float phase = rand(vec2(slice, 0.0));
        	// float dist = rand(vec2(slice, 1.0)) * 3.0;
        	// float hue = rand(vec2(slice, 2.0));
        
        	float z = dist / length(v.xy) * v.z;
        	float Z = mod(z + phase + iTime * xxh, xxi);
        	// float Z = mod(z + phase + iTime * 0.6, 1.0);
        	float d = sqrt(z * z + dist * dist);
        
        	float c = exp(-Z * xxj + 0.3) / (d * d + 1.0);
        	// float c = exp(-Z * 8.0 + 0.3) / (d * d + 1.0);
        	fragColor = vec4(hsv(hue, 0.6 * (1.0 - clamp(2.0 * c - 1.0, 0.0, 1.0)), clamp(2.0 * c, 0.0, 1.0)), 1.0);
        
    	    vec4 factor = mix(texColorX,fragColor,1.0);
    	    // const vec3 luminanceWeighting = vec3(0.2126, 0.7152, 0.0722);
            // float luminance = dot(fragColor.rgb, luminanceWeighting);
            // vec4 gradmap = mix(color1,color2,luminance)*fragColor.a;
    	    // if(GRADMAP) { factor = mix(texColorX,gradmap,1.0); }
    	    
    	    // vec4 xbuffer = texture2DCv(buff1.texture, acScreenNorm.st);

        vec4 resultfinal;
          if( blendMode==0 ) { resultfinal = (factor * hardness) * texColor.a; }
          else if( blendMode==1 ) { factor *= hardness;
            vec4 c = factor + vec4(1.)*(1.-factor.a);
            resultfinal = vec4((texColor.rgb/texColor.a)*c.rgb,1.0) * texColor.a; }
            // resultfinal = min(factor, texColor) * texColor.a; }
          else if( blendMode==2 ) { factor *= hardness; 
            resultfinal = vec4( 1.-((1.-(texColor.rgb/texColor.a))*(1.-factor.rgb)),1.0) * texColor.a; }
              // resultfinal = max(factor, texColor) * texColor.a; }
          else if( blendMode==3 ) { factor *= hardness; resultfinal = (newLayer(texColor, factor) * hardness) * texColor.a; }
            
    	gl_FragColor = mix(texColor, resultfinal, alpha) * texColor.a;
    	// }
    }


    </shader>

</effect>