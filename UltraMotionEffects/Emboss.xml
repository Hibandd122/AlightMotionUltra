<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.effects.extra2.emboss" name="Emboss" desc="Carves the layer into a raised metallic relief by emphasizing edge direction." category="drawingedge" thumb="thumb/Stylize/Emboss.webp" tags="stylize,emboss,relief,raster">

    <params>
        <texture id="inputImg" srcType="content" />
        <slider id="strength" default="1.0" min="0.0" max="3.0" label="Strength" />
        <slider id="amount" default="1.0" min="0.0" max="1.0" label="Amount" type="percent" />
    </params>

    <shader type="fragment" precision="high">
        void main(){
            vec2 px = 1.0/acScreenSize;
            vec4 src = texture2DCv(inputImg.texture, acScreenNorm);
            vec3 c = src.a&gt;0.0? src.rgb/src.a : src.rgb;
            vec3 a = texture2DCv(inputImg.texture, acScreenNorm-px).rgb;
            vec3 b = texture2DCv(inputImg.texture, acScreenNorm+px).rgb;
            vec3 e = (a-b)*strength + 0.5;
            float g = dot(e, vec3(0.333));
            gl_FragColor = vec4(mix(c, vec3(g), amount),1.0)*src.a;
        }
    </shader>

</effect>