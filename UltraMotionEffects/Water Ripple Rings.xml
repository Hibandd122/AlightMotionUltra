<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.effects.smallringswater" name="Water Ripple Rings" desc="Simulates rain ripples: tiled cells spawn expanding concentric ring waves at random positions and times, layered over several iterations. Controls include Amount, Rain Speed, Sharpness, Expand Speed, Num Rings and Strength." category="procedural" tags="other" thumb="thumb/Procedural/Water Ripple Rings.webp" experimental="false">

    <params>
        <slider id="numIterations" label="Amount" default="4" min="0" max="5" step="1.0" type="integer" />
        <switch id="autop" default="true" label="Auto Progress" />
        <slider id="PROGRESS" label="Progress" default="1.0" min="-100.0" max="100.0" step="0.01" type="percent" />
        <slider id="rainSpeed" label="Rain Speed" default="0.15" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="sharpnessx" label="Sharpness" default="4.5" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="expansionSpeed" label="Expand Speed" default="4.0" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="numRings" label="Num Rings" default="3.0" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="strength" label="Strength" default="0.3" min="0.0" max="100.0" step="0.01" type="percent" />
        
        <slider id="scale" default="1.0" min="0.1" max="50.0" step="0.01" label="Scale" type="percent" />
        <slider id="angle" default="0.0" min="-3600" max="3600" ticks="-180,-135,-90,-45,-30,-15,0,15,30,45,90,135,180" label="Rotate" step="0.5" type="angle" />
        <point id="position" label="@am:string/effect_param_offset" />
        <point id="stretch" default="1.0,1.0" min="0.01,0.01" max="10, 10" label="@am:string/effect_param_stretch" step="0.01" />
        <slider id="alpha" default="1.0" min="0.0" max="1.0" step="0.01" label="Alpha" type="percent" />
        <slider id="hardness" default="1.0" min="1.0" max="3.0" step="0.01" label="Hard/Brightness" type="percent" />
        <menu id="blendMode" style="radio" default="3">
            <menu label="NORMAL" value="0" />
            <menu label="LAYER" value="3" />
            <menu label="MULTIPLY" value="1" />
            <menu label="SCREEN" value="2" />
        </menu>
    <texture id="inputImg" srcType="content" />
    <texture id="xcipher" srcType="buffer" downsample="2" />
    </params>
    
    <passes>
        <pass target="xcipher" />
        <pass />
    </passes>
    
    

    <shader type="fragment">
    //////////////////////////
    vec4 newLayer( vec4 a, vec4 b) { return a * (1. - b.a) + b;  }
    mat2 rot2m(float a){ float s = sin(a); float c = cos(a); return mat2(c, -s, s, c); }
    vec2 l2s( vec2 v ) { return (vec4(v,0.,1.)*acLayerToScreen).xy; }
    #define fragCoord gl_FragCoord.xy
    
   /////////////////////////
   
        vec2 randVec(float inVal){
            
            return vec2(fract(sin(dot(vec2(inVal*1.1,2352.75053) ,vec2(12.9898,78.233))) * 43758.5453)-0.5,
                        fract(sin(dot(vec2(715.23515, inVal) ,vec2(27.2311,31.651))) * 65161.6513)-0.5);
                        
        }
        
        float randFloat(vec2 inVal){
            return fract(sin(dot(vec2(inVal.x, inVal.y) ,vec2(89.4516,35.516))) * 13554.3651);
                        
        }
    //////////////////////////
        void main() {
        if( acPass == 0 ) { gl_FragColor = texture2DCv(inputImg.texture, acScreenNorm.st); } else if( acPass==1 ) {
        vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
        vec4 texColorX = vec4(0.0,0.0,0.0,1.0) * texColor.a;
        
        vec2 image_size = getTexSize(inputImg.size);
        vec2 iResolution = vec2(acScreenSize.x, acScreenSize.y);
        vec2 iMouse = vec2(0.5, 0.5);
        float iTime = acTime;
        
        vec2 umefx;
        umefx = ((vec2(acScreenNorm.x,1.0-acScreenNorm.y)*acScreenSize)-acLayerCenter)/acLayerSize;
        umefx.y*=acLayerSize.y/acLayerSize.x;
        umefx /= scale;
        umefx *= rot2m((0.0+angle)*0.0174533);
        umefx += vec2((0.0+position.x), (0.0-position.y))*vec2(-1.,1.)/500.;
        vec2 scaleVec;
        scaleVec = vec2(scale*stretch.x,scale*stretch.y);
        umefx /= scaleVec;
        
        /////////////////////////
        
    	vec4 fragColor;
    	
            float zoom = autop? sin(PROGRESS + (iTime*0.5)) : sin(PROGRESS*0.1);
            zoom = 0.9+pow(((iMouse.x+iMouse.y)/iResolution.x)*1.,3.0); 
            
            float sharpness = sharpnessx*zoom; // maybe plug in ddx and ddy here to mimic mip-mapping (avoid artifacts at long distances)
            // float expansionSpeed = 4.0;
            // float rainSpeed = 0.6;
            // float numRings = 3.0;
            // const float numIterations = 4.0;
            // float strength = 0.3;
            
            // other numbers:
            const float pi = 3.141592;
            float newTime = autop? PROGRESS + (iTime*rainSpeed) : PROGRESS*rainSpeed;
            
            vec2 uv;
            vec2 uvStep;
            fragColor = vec4(0.);
            for(float iterations = 0.; iterations &lt; numIterations; iterations++){
                for(float xpos = -1.;xpos&lt;=1.;xpos++){
                    for(float ypos = -1.;ypos&lt;=1.;ypos++){
                        uv = umefx;
                        // uv = (2.*fragCoord.xy - iResolution.xy) / iResolution.y;
                        uv /= zoom;
                        uv += iterations*vec2(3.21,2.561);
                        uv += vec2(xpos*0.3333,ypos*0.3333);
                        uvStep = (ceil((uv*1.0-vec2(.5,.5)))/1.);
                        uvStep += vec2(xpos,ypos)*100.;
                        uv = vec2(fract(uv.x+0.5)-.5,fract(uv.y+0.5)-.5);
        
                        // Variables:
                        float timeRand = randFloat(uvStep);
                        float timeLoop = fract(newTime+timeRand);
                        float timeIter = floor(newTime+timeRand);
        
        
                        /// Creating ringMap:
                        float ringMap = sharpness*9.*distance(uv, randVec(timeIter+uvStep.x+uvStep.y)*0.5);
                        //float ringMap = sharpness*9.*distance(uv, randVec(0.)*0.);
                        float clampMinimum = -(1.+((numRings-1.)*2.0));
                        ringMap = clamp((ringMap-expansionSpeed*sharpness*(timeLoop))+1., clampMinimum, 1.);
        
                        // Rings and result
                        float rings = (cos((ringMap+newTime)*pi)+1.0)/2.;
                        rings *= pow(1.-timeLoop,2.);
                        float bigRing = sin((ringMap-clampMinimum)/(1.-clampMinimum)*pi);
                        float result = rings * bigRing;
                        fragColor += vec4(result)*strength;
                    }
                }
            }
        
    	vec4 factor = mix(texColorX,fragColor,1.0);
    	
        vec4 resultfinal;
          if( blendMode==0 ) {
            resultfinal = (factor * hardness) * texColor.a; }
            // resultfinal = (newLayer(texColor, factor) * hardness) * texColor.a; }
          else if( blendMode==1 ) { factor *= hardness;
            vec4 c = factor + vec4(1.)*(1.-factor.a);
            resultfinal = vec4((texColor.rgb/texColor.a)*c.rgb,1.0) * texColor.a; }
            // resultfinal = min(factor, texColor) * texColor.a; }
          else if( blendMode==2 ) { factor *= hardness; 
            resultfinal = vec4( 1.-((1.-(texColor.rgb/texColor.a))*(1.-factor.rgb)),1.0) * texColor.a; }
          else if( blendMode==3 ) { factor *= hardness; 
            resultfinal = (newLayer(texColor, factor) * hardness) * texColor.a; }
              // resultfinal = max(factor, texColor) * texColor.a; }
            
    	gl_FragColor = mix(texColor, resultfinal, alpha) * texColor.a;
          }
        }

    </shader>

</effect>