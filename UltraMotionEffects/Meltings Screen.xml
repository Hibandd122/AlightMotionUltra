<?xml version='1.0' encoding='utf-8'?>
<effect category_ultra="Shake & Transform" id="com.alightcreative.effects.meltings" name="Meltings Screen" desc="Melts the layer downward over time by displacing UVs with three sinusoidal waves (each with its own amplitude and frequency) plus an accelerating gravity drip. Auto or manual progress, transform controls, and an overdraw switch; the animation loops every few seconds." category="transform" tags="distort" thumb="thumb/12/20.png" experimental="false" overdraw="true" max-overdraw="1.0">

    <params>
        
        <switch id="autop" default="true" label="Auto Progress" />
        <switch id="overdraw" default="true" label="Overdraw" />
        <slider id="PROGRESS" label="Progress" default="1.0" min="-100.0" max="100.0" step="0.01" type="percent" />
        <slider id="SPEED" label="Speed AutoProg" default="1.0" min="0.0" max="100.0" step="0.01" type="percent" />
        
        <slider id="GRV" label="Gravity" default="3.0" min="0.0" max="100.0" step="0.01" type="percent" />
        
        <slider id="W1" label="Wave 1" default="0.5" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="W2" label="Wave 2" default="1.3" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="W3" label="Wave 3" default="2.4" min="0.0" max="100.0" step="0.01" type="percent" />
        
        <slider id="F1" label="Freq 1" default="0.2" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="F2" label="Freq 2" default="0.1" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="F3" label="Freq 3" default="0.05" min="0.0" max="100.0" step="0.01" type="percent" />
        
        <slider id="scale" default="1.0" min="0.1" max="50.0" step="0.01" label="Scale" type="percent" />
        <slider id="angle" default="0.0" min="-3600" max="3600" ticks="-180,-135,-90,-45,-30,-15,0,15,30,45,90,135,180" label="Rotate" step="0.5" type="angle" />
        <point id="position" label="@am:string/effect_param_offset" />
        <point id="stretch" default="1.0,1.0" min="0.01,0.01" max="10, 10" label="@am:string/effect_param_stretch" step="0.01" />
        <slider id="alpha" default="1.0" min="0.0" max="1.0" step="0.01" label="Alpha" type="percent" />
        <slider id="hardness" default="1.0" min="1.0" max="3.0" step="0.01" label="Hard/Brightness" type="percent" />
        <menu id="blendMode" style="radio" default="3">
            <menu label="NORMAL" value="0" />
            <menu label="LAYER" value="3" />
            <menu label="MULTIPLY" value="1" />
            <menu label="SCREEN" value="2" />
        </menu>
        
        
        
        <texture id="inputImg" srcType="content" />
        <texture id="buff1" srcType="buffer" />

    </params>
    
    <passes><pass target="buff1" /><pass /></passes>
    
    <shader type="fragment" precision="high">
    
    vec4 newLayer( vec4 a, vec4 b) { return a * (1. - b.a) + b;  }
    mat2 rot2m(float a){ float s = sin(a); float c = cos(a); return mat2(c, -s, s, c); }
    vec2 l2s( vec2 v ) { return (vec4(v,0.,1.)*acLayerToScreen).xy; }
    #define fragCoord gl_FragCoord.xy
    
   /////////////////////////
   

   
   /////////////////////////
    void main ()
    {
            
        vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
        vec4 texColorX = vec4(0.0,0.0,0.0,1.0) * texColor.a;
        
        vec2 image_size = getTexSize(inputImg.size);
        vec2 iResolution = vec2(acScreenSize.x, acScreenSize.y);
        vec3 iMouse = vec3(0.5, 0.5, 0.5);
        float iTime = autop? PROGRESS + (acTime*SPEED) : PROGRESS;
        
        vec2 umefx;
        umefx = acScreenNorm; 
        // umefx = ((vec2(acScreenNorm.x,1.0-acScreenNorm.y)*acScreenSize)-acLayerCenter)/acLayerSize;
        // umefx.y*=acLayerSize.y/acLayerSize.x;
        
        umefx /= scale;
        umefx *= rot2m((0.0+angle)*0.0174533);
        umefx += vec2((0.0+position.x), (0.0+position.y))*vec2(-1.,1.)/500.;
        vec2 scaleVec;
        scaleVec = vec2(scale*stretch.x,scale*stretch.y);
        umefx /= scaleVec;
        
        /////////////////////////
        
    	vec4 fragColor;
    	
    	    vec4 O;
             float time = iTime;
             time = mod(time, 3.5);//comment out this line if you dont want it to repeat
             if (time &gt; 3.5)
                 O = vec4(0.0,0.0,0.0,0.0);
             else
             {
                 vec2 p = umefx;
                 // vec2 p = uv.xy / iResolution.xy;
                 float melt = time * 0.2;
                 float stretch = sin(((p.x*W1)) * 10.0) * time * F1;
                 stretch += sin(((p.x*W2)) * 10.0) * time * F2;
                 stretch += sin(((p.x*W3)) * 10.0) * time * F3;
                 // float stretch = sin(((p.x*0.5)) * 10.0) * time * 0.2;
                 // stretch += sin(((p.x*1.3)) * 10.0) * time * 0.1;
                 // stretch += sin(((p.x*2.4)) * 10.0) * time * 0.05;
                 vec2 offset = vec2(0., (stretch * stretch) + ((melt * melt * melt) * GRV));
                 p += offset;
                 vec4 video = vec4(texture2DCv(inputImg.texture,p));
                 if (p.y &gt; 1.0 || p.x &lt; 0.0 || p.x &gt; 1.0 || p.y &lt; 0.0)
                     O = vec4(0.0,0.0,0.0,0.0);
                 else
                     O = video;
             }
        
            
    	gl_FragColor = overdraw?O : O * texColor.a;
    	// gl_FragColor = mix(texColor, resultfinal, alpha) * texColor.a;
    	// }
    }


    </shader>

</effect>