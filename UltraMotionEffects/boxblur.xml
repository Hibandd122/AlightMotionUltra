<?xml version='1.0' encoding='utf-8'?>
<effect category_ultra="Blur, Focus & Sharpen" id="com.alightcreative.effects.boxblur" name="@string/name" desc="@string/desc" category="blur" thumb="thumb/box_blur.webp" deprecated="true">

    

    <params>
        <texture id="inputImg" srcType="content" />
        <texture id="hblur" srcType="buffer" />
        <slider id="strength" default="1.0" min="0.0" max="100.0" label="@string/strength" type="integer" step="1" />
        <slider id="iter" default="1" min="1" max="9" label="@string/iterations" type="integer" step="1" />
    </params>

    <iterations param="iter" />

    <passes>
        <pass target="hblur" />
        <pass />
    </passes>

    <shader type="fragment">

        void main() {
            int from = -max(1,int(strength));
            int to = max(1,int(strength));
            vec4 outColor = vec4(0.0);
            if( acPass==0 ) {
                for(int i=from; i&lt;to; i++ ) {
                    outColor += texture2DCv(inputImg.texture,vec2(acScreenNorm.s + float(i)*(1.0/1280.0),acScreenNorm.t));
                }
            } else {
                for(int i=from; i&lt;to; i++ ) {
                    outColor += texture2DCv(hblur.texture,vec2(acScreenNorm.s,acScreenNorm.t + float(i)*(1.0/720.0)));
                }
            }
            gl_FragColor = outColor/(float(to)*2.0);
        }

    </shader>

</effect>