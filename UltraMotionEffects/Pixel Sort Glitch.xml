<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.effects.pixelsorter" name="Pixel Sort Glitch" desc="Imitates a pixel-sorting glitch by shifting each column of pixels up or down by a random amount, with the direction chosen by a luminance threshold. Progress, max offset, seed, smoothing and tilt shape the streaks." category="procedural" tags="distort,glitch" thumb="thumb/4/pixel.png" experimental="false">

    <params>
        <texture id="inputImg" srcType="content" />
        <slider id="progressx" label="Progress" default="1.0" min="1.0" max="3.0" step="0.01" type="percent" />
        <slider id="progressb" label="Y Offset" default="0.0" min="-2000.0" max="2000.0" step="5.0" />
        <slider id="deltax" default="1.0" min="0.0" max="5.0" step="0.01" label="Seed" type="percent" />
        <slider id="pixel" default="0.025" min="0.0" max="1.0" step="0.001" label="Smoothing" type="percent" />
        <slider id="angle" default="0.0" min="-360" max="360" ticks="-180,-135,-90,-45,-30,-15,0,15,30,45,90,135,180" label="Tilt" step="0.5" type="angle" />
        <slider id="alpha" default="1.0" min="0.0" max="1.0" step="0.01" label="Alpha" type="percent" />
        <slider id="maxoffset" label="Max Offset" default="80.0" min="-100.0" max="100.0" step="1.0" snap="80.0" ticks="80.0" />
        <switch id="offside" default="false" label="Offside" />

    </params>
    
    <shader type="fragment" precision="high">
    
    vec4 newLayer( vec4 a, vec4 b) {
    return a * (1. - b.a) + b;
    }
    
    mat2 rot2m(float a){
        
	float s = sin(a);
	float c = cos(a);
	
	return mat2(c, -s, s, c);
    }
    
    vec2 l2s( vec2 v ) {
        return (vec4(v,0.,1.)*acLayerToScreen).xy;
    }

    
   /////////////////////////
    
    #define MAX_OFFSET maxoffset
    #define vertoffs false
    #define mirror true
    #define phase 0.0
    #define progress (1.57 * progressx)
    
     float rand(float co) { return fract(sin(co*(91.3458+deltax)) * 47453.5453); }

    void main ()
    {
            
        vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
                        
        /////////////////////////
        
    	vec4 result;
    	        
    	//////////////////////////
    	
    	vec2 uv = acScreenNorm;
    	
        vec2 texel = 1. / acScreenNorm;
        texel *= rot2m(angle*0.0174533);
        texel += (0.0,progressb)*vec2(-1.,1.)/500.;
        
        vec4 img = texColor;
        
        // float step_y;
        // if( stepy ) { step_y = texel.y*(rand(uv.x)*100.0); }
        // else { step_y = texel.y*(rand(uv.x)*MAX_OFFSET) * (sin(sin(-1.57*0.5))*2.0+1.3); }
        
        float step_y = texel.y*(rand(uv.x)*MAX_OFFSET) * (sin(sin(-1.57*0.5))*2.0+1.3);
        step_y += rand(uv.x*uv.y*1.0)*pixel*sin(1.0);								// shake offset and modulate it
        step_y = mix(step_y, step_y*rand(uv.x*0.0)*0.5, sin(progress)); 					// more noisy spikes
        
        if ( dot(img,  vec4(0.299, 0.587, 0.114, 0.0) ) &gt; 1.2*(sin(1.0)*0.325+0.50)){
        	uv.y+=step_y;
        } else{
        	uv.y-=step_y;
        }
        
        img = texture2DCv(inputImg.texture, uv);
        
        //////////////////////////
        
        result = mix(texColor,img,1.0) * texColor.a;
        if ( offside) { result = mix(texColor,img,1.0); }
        
        gl_FragColor = newLayer(texColor,result);
        
    }


    </shader>

</effect>