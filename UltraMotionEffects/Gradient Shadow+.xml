<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.effects.gradshadow" name="Gradient Shadow+" desc="Overlays an angled linear gradient of a single color across the layer with feathered start (Progress) and end (Offset) edges, composited with 25 selectable blend modes and adjustable brightness and alpha." category="drawingedge" tags="other" thumb="thumb/Color/Gradient Shadow+.webp" experimental="false">

    <params>
        <texture id="inputImg" srcType="content" />
        <color id="color" default="#FF000000" label="Color" alpha="true" />
        <slider id="start" default="0.95" min="-2.0" max="2.0" step="0.01" label="Progress" type="percent" />
        <slider id="angle" default="90.0" min="0.0" max="360.0" label="@am:string/effect_param_angle" type="angle" step="0.5" ticks="0,45,90,135,180,225,270,315,360" snap="45,90,135" />
        <slider id="alphb" default="1.0" min="0.0" max="1.0" step="0.01" label="Alpha" type="percent" />
        
        <slider id="feather" default="100.0" min="0.0" max="100.0" step="0.5" label="@am:string/effect_param_feather" />
        <slider id="end" default="1.5" min="-2.0" max="2.0" step="0.01" label="Offset" type="percent" />
        
            
            
            
        
       <slider id="bright" default="1.0" min="1.0" max="3.0" step="0.01" label="Brightness" type="percent" />
       <menu id="blendMode" style="dropdown" default="0" label="@am:string/effect_param_blendmode">
            <menu label="NORMAL" value="0" />
            <menu label="MULTIPLY" value="1" />
            <menu label="SCREEN" value="2" />
            <menu label="OVERLAY" value="3" />
            <menu label="COLOR" value="4" />
            <menu label="LIGHTEN" value="5" />
            <menu label="LIGHTEN COLOR" value="13" />
            <menu label="COLOR DODGE" value="6" />
            <menu label="COLOR BURN" value="7" />
            <menu label="LINEAR DODGE" value="8" />
            <menu label="LINEAR BURN" value="9" />
            <menu label="LINEAR LIGHT" value="10" />
            <menu label="DARKEN" value="11" />
            <menu label="DARKEN COLOR" value="12" />
            <menu label="SOFT LIGHT" value="14" />
            <menu label="SOFT OVERLAY" value="15" />
            <menu label="HARD LIGHT" value="16" />
            <menu label="VIVID LIGHT" value="21" />
            <menu label="PIN LIGHT" value="22" />
            <menu label="HUE" value="17" />
            <menu label="DIFFERENT" value="18" />
            <menu label="LUMINANCE" value="19" />
            <menu label="SATURATION" value="20" />
            <menu label="DIVIDE" value="23" />
            <menu label="SUBSTRACT" value="24" />
        </menu>
        <slider id="alphab" default="1.0" min="0.0" max="1.0" step="0.01" label="Alpha Blend" type="percent" />

    </params>
    
    <shader type="fragment" precision="high">
    
    mat2 rot2m(float a){
        
	float s = sin(a);
	float c = cos(a);
	
	return mat2(c, -s, s, c);
    }
    
    vec4 newLayer( vec4 a, vec4 b) {
    return a * (1. - b.a) + b;
    }
    
   /////////////////////////
    
   vec2 project(vec2 a, vec2 b) {
            return (dot(a,b)/dot(b,b)) * b;
        }

        float smoothstepRev(float edge0, float edge1, float x) {
            if(edge0 == edge1) return step(edge0, x);
            else return smoothstep(edge0, edge1, x);
        }
    
        const vec3 luminanceWeighting = vec3(0.2126, 0.7152, 0.0722);
   
        vec3 hsv2rgb(vec3 c)
        {
            vec4 K = vec4(1.0, 2.0 / 3.0, 1.0 / 3.0, 3.0);
            vec3 p = abs(fract(c.xxx + K.xyz) * 6.0 - K.www);
            return c.z * mix(K.xxx, clamp(p - K.xxx, 0.0, 1.0), c.y);
        }

        vec3 rgb2hsv(vec3 c)
        {
            vec4 K = vec4(0.0, -1.0 / 3.0, 2.0 / 3.0, -1.0);
            vec4 p = mix(vec4(c.bg, K.wz), vec4(c.gb, K.xy), step(c.b, c.g));
            vec4 q = mix(vec4(p.xyw, c.r), vec4(c.r, p.yzx), step(p.x, c.r));

            float d = q.x - min(q.w, q.y);
            float e = 1.0e-10;
            return vec3(abs(q.z + (q.w - q.y) / (6.0 * d + e)), d / (q.x + e), q.x);
        }
            highp mat3 rgb2yuv = mat3(
                 0.299,     0.587,    0.114,
                -0.14713,  -0.28886,  0.436,
                 0.615,    -0.51499, -0.10001
            );
            highp mat3 yuv2rgb = mat3(
                1.0,  0.0,      1.13983,
                1.0, -0.39465, -0.58060,
                1.0,  2.03211,  0.0
            );
    
    void main ()
    {
        
        // highp mat3 rgb2yuv = mat3(
                 // 0.299,     0.587,    0.114,
                // -0.14713,  -0.28886,  0.436,
                 // 0.615,    -0.51499, -0.10001
            // );
            // highp mat3 yuv2rgb = mat3(
                // 1.0,  0.0,      1.13983,
                // 1.0, -0.39465, -0.58060,
                // 1.0,  2.03211,  0.0
            // );
            // float radius = hue * 0.0174533;
            // highp mat3 huemat = mat3(
                // 1.0,  0.0,       0.0,
                // 0.0,  cos(radius),  sin(radius),
                // 0.0,  -sin(radius), cos(radius)
            // );
            
        vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
        vec2 umefx = acLayerNorm;
    	
        umefx -= 0.5;
        umefx += 0.5;
        umefx.x *= acLayerSize.x/acLayerSize.y;
        umefx.x /= acLayerSize.x/acLayerSize.y;
        umefx /= 1.0;
        // umefx *= rot2m(angle*0.0174533);
        // umefx += position*vec2(-1.,1.)/500.;
                        
        /////////////////////////
        
        
    	vec4 result;
    	
    	float adjStart = start;
            float adjEnd = end;
            float adjFeather = feather/100.0;
            float rad = angle * 0.0174533;
            vec2 v = normalize(vec2(cos(rad),-sin(rad)));
            //vec2 v = (vec4(normalize(vec2(cos(rad),-sin(rad))),0.0,1.0) * acLayerToScreen).xy;
            //float p = length(projectAndClamp(acLayerNorm.st-vec2(0.5,0.5), v))/length(v)+0.5;
            float p = dot(acLayerNorm.st-vec2(0.5,0.5), v)+0.5;

            float p0 = smoothstepRev(adjStart-adjFeather,adjStart,p);
            float p1 = 1. - smoothstepRev(adjEnd,adjEnd+adjFeather,p);
            
            result = texture2DCv(inputImg.texture,acScreenNorm.st)*p0*p1;
            
            vec4 rs = vec4(1.,1.,1.,1.) *p0*p1 * texColor.a * alphb;
    
            vec4 fragColor = rs * color;
    	
        vec4 texColorX = vec4(0.0,0.0,0.0,1.0) * texColor.a;
    	vec4 factor = mix(texColorX,fragColor,1.0);

        vec4 resultfinal;
        if( blendMode==0 ) {                // normal
            resultfinal = newLayer(texColor, (factor*bright)) * texColor.a;
        } else if( blendMode==1 ) {         // multiply
            factor *= bright;
            vec4 c = factor + vec4(1.)*(1.-factor.a);
            resultfinal = vec4((texColor.rgb/texColor.a)*c.rgb,1.0) * texColor.a;
        } else if( blendMode==2 ) {         // screen
            factor *= bright;
            resultfinal = vec4( 1.-((1.-(texColor.rgb/texColor.a))*(1.-factor.rgb)),1.0) * texColor.a;
        } else if(blendMode==3 ) {
            factor *= bright;
            vec3 t = smoothstep(0.5,0.5,texColor.rgb);
            vec3 result =
                    t * (1. - (1.-2.*(texColor.rgb-0.5)) * (1.-factor.rgb)) +
                    (1.-t) * ((2.*texColor.rgb) * factor.rgb);
            resultfinal = vec4(result, texColor.a) * texColor.a;
        } else if(blendMode==4 ) {
            vec3 colorBlend = vec3( (texColor.rgb * rgb2yuv).r, (factor.rgb * rgb2yuv).gb) * yuv2rgb;
            resultfinal = vec4(colorBlend,texColor.a) * texColor.a;
        } else if(blendMode==5 ) {
            factor *=bright;
            resultfinal = max(factor,texColor) * texColor.a;
        } else if(blendMode==6 ) {
            factor *=bright;
            resultfinal = texColor / (1.0-factor) * texColor.a;
        } else if(blendMode==7 ) {
            factor *=bright;
            resultfinal = (1. - (1.-texColor) / factor) * texColor.a;
        } else if(blendMode==8 ) {
            factor *=bright;
            resultfinal = (texColor + factor) * texColor.a;
        } else if(blendMode==9 ) {
            factor *=bright;
            resultfinal = (texColor + factor - 1.0) * texColor.a;
        } else if(blendMode==10 ) {
            vec3 t = smoothstep(0.5,0.5,factor.rgb);
            vec3 linearLight =
                    t * (texColor.rgb + 2.*(factor.rgb-0.5)) +
                    (1.-t) * (texColor.rgb + 2.*factor.rgb - 1.);
            resultfinal = vec4(linearLight,texColor.a) * texColor.a;
        } else if(blendMode==11 ) {
            factor *=bright;
            resultfinal = min(factor,texColor) * texColor.a;
        } else if(blendMode==12 ) {
            factor *=bright;
            vec3 darkenColor = length(luminanceWeighting * factor.rgb) &gt; length(luminanceWeighting * texColor.rgb) ? texColor.rgb : factor.rgb;
            resultfinal = vec4(darkenColor,texColor.a) * texColor.a;
        } else if(blendMode==13 ) {
            factor *=bright;
            vec3 lightenColor = length(luminanceWeighting * factor.rgb) &gt; length(luminanceWeighting * texColor.rgb) ? factor.rgb : texColor.rgb;
            resultfinal = vec4(lightenColor,texColor.a) * texColor.a;
        } else if(blendMode==14 ) {
            factor *=bright;
            vec3 t = smoothstep(0.5,0.5,factor.rgb);
            vec3 softLight =
                    t * (1. - (1.-texColor.rgb) * (1.-(factor.rgb-0.5))) +
                    (1.-t) * (texColor.rgb * (factor.rgb+0.5));
            resultfinal = vec4(softLight,texColor.a) * texColor.a;
        } else if(blendMode==15 ) {
            factor *=bright;
            vec3 t = smoothstep(0.5,0.5,texColor.rgb);
            vec3 softOver =
                    t * (1. - (1.-texColor.rgb) * (1.-(factor.rgb-0.5))) +
                    (1.-t) * (texColor.rgb * (factor.rgb+0.5));
            resultfinal = vec4(softOver,texColor.a) * texColor.a;
        } else if(blendMode==16 ) {
            factor *=bright;
            vec3 t = smoothstep(0.5,0.5,factor.rgb);
            vec3 hardLight =
                    t * (1. - (1.-texColor.rgb) * (1.-2.*(factor.rgb-0.5))) +
                    (1.-t) * (texColor.rgb * (2.*factor.rgb));
            resultfinal = vec4(hardLight,texColor.a) * texColor.a;
        } else if(blendMode==17 ) {
            factor *=bright;
            vec3 hueB = hsv2rgb(vec3(rgb2hsv(factor.rgb).r, rgb2hsv(texColor.rgb).gb));
            resultfinal = vec4(hueB,texColor.a) * texColor.a;
        } else if(blendMode==18 ) {
            factor *=bright;
            resultfinal = vec4(abs(texColor.rgb*texColor.a - factor.rgb),texColor.a);
        } else if(blendMode==19 ) {
            factor *=bright;
            vec3 lumin = vec3( (factor.rgb * rgb2yuv).r, (texColor.rgb * rgb2yuv).gb) * yuv2rgb;
            resultfinal = vec4(lumin,texColor.a) * texColor.a;
        } else if(blendMode==20 ) {
            factor *=bright;
            vec3 bot_hsv = rgb2hsv(texColor.rgb);
            vec3 satBlend = hsv2rgb(vec3(bot_hsv.r, rgb2hsv(factor.rgb).g, bot_hsv.b));
            resultfinal = vec4(satBlend,texColor.a) * texColor.a;
        } else if(blendMode==21 ) {
            factor *=bright;
            vec3 t = smoothstep(0.5,0.5,factor.rgb);
            vec3 vividLight =
                    t * (1. - (1.-texColor.rgb) * (2.*(factor.rgb-0.5))) +
                    (1.-t) * (texColor.rgb * (1.-2.*factor.rgb));
            resultfinal = vec4(vividLight,texColor.a) * texColor.a;
        } else if(blendMode==22 ) {
            factor *=bright;
            vec3 t = smoothstep(0.5,0.5,factor.rgb);
            vec3 pinLight =
                    t * (max(texColor.rgb,2.*(factor.rgb-0.5))) +
                    (1.-t) * (min(texColor.rgb,2.*factor.rgb));
            resultfinal = vec4(pinLight,texColor.a) * texColor.a;
        } else if(blendMode==23 ) {
            factor *=bright;
            resultfinal = (texColor / factor) * texColor.a;
        } else if(blendMode==24 ) {
            factor *=bright;
            resultfinal = (texColor - factor) * texColor.a;
        }
            
    	gl_FragColor = mix(fragColor, resultfinal, alphab) * texColor.a;
    }


    </shader>

</effect>