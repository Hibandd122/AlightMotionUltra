<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.effects.meteor" name="Meteor" desc="Draws columns of falling light streaks (meteor/rain trails) with per-column randomized speed, offset, and trail length, plus sine-wave distortion of the field. Controls include wave X/Y, trail length, mirror X/Y, base and background colors, transform, and blend modes." category="procedural" tags="other" thumb="thumb/5/meteor.png" experimental="false">

    <params>
    
        <switch id="autop" default="true" label="Auto Progress" />
        <slider id="PROGRESS" label="Progress" default="1.0" min="-10.0" max="100.0" step="0.01" type="percent" />
        <slider id="xxa" label="Wave X" default="0.5" min="-10.0" max="100.0" step="0.01" type="percent" />
        <slider id="xxx" label="Wave Y" default="0.5" min="-10.0" max="100.0" step="0.01" type="percent" />
        <slider id="xxc" label="Lenght" default="0.15" min="-10.0" max="100.0" step="0.01" type="percent" />
        <slider id="scale" default="0.5" min="0.1" max="50.0" step="0.01" label="Scale" type="percent" />
        <point id="position" label="@am:string/effect_param_offset" />
        <point id="stretch" default="1.0,1.0" min="0.01,0.01" max="10, 10" label="@am:string/effect_param_stretch" step="0.01" />
        <slider id="bright" default="1.0" min="0.5" max="3.0" step="0.01" label="Hardness" type="percent" />
        <slider id="alpha" default="1.0" min="0.0" max="1.0" step="0.01" label="Alpha" type="percent" />
        <slider id="angle" default="0.0" min="-3600" max="3600" ticks="-180,-135,-90,-45,-30,-15,0,15,30,45,90,135,180" label="Rotate" step="0.5" type="angle" />
        <color id="color1" default="#FF00FFFF" label="Color Base" alpha="true" />
        <color id="color2" default="#00000000" label="Color BG" alpha="true" />
        <switch id="mirrx" default="false" label="Mirror X" />
        <switch id="mirry" default="true" label="Mirror Y" />
        <menu id="blendMode" style="radio" default="2" label="@am:string/effect_param_blendmode">
            <menu label="NORMAL" value="0" />
            <menu label="MULTIPLY" value="1" />
            <menu label="SCREEN" value="2" />
        </menu>
        <texture id="inputImg" srcType="content" />

    </params>
    
    <shader type="fragment" precision="high">
    
    vec4 newLayer( vec4 a, vec4 b) {
    return a * (1. - b.a) + b;
    }
    
    mat2 rot2m(float a){
        
	float s = sin(a);
	float c = cos(a);
	
	return mat2(c, -s, s, c);
    }
    
    vec2 l2s( vec2 v ) {
        return (vec4(v,0.,1.)*acLayerToScreen).xy;
    }
    
   #define fragCoord gl_FragCoord.xy
    
   /////////////////////////
   
        #define PI 3.14159
        #define TAU 6.28318
        
        vec3 spunk(vec2 uv)
        {
          vec3 col = vec3(color1.rgb);    // Drop Colour
          // vec3 col = vec3(.55,0.35,1.225);    // Drop Colour
          uv.x += sin(0.2+uv.y*0.8)*xxa;
          uv.y += sin(0.2+uv.x*0.8)*xxx;
          // uv.x += sin(0.2+uv.y*0.8)*0.5;
            uv.x = uv.x*50.0;           // H-Count
            float dx = fract(uv.x);
            uv.x = floor(uv.x);
            float t =  autop? PROGRESS+(acTime*0.5) : PROGRESS * 0.5;
            uv.y *= xxc;             // stretch
            // uv.y *= 0.15;             // stretch
            float o=sin(uv.x*215.4);        // offset
            float s=cos(uv.x*33.1)*0.3 + 0.7;      // speed
            float trail = mix(95.0,35.0,s);     // trail length
            float yv = fract(uv.y + t*s + o) * trail;
            yv = 1.0/yv;
            yv = smoothstep(0.0,1.0,yv*yv);
            yv = sin(yv*PI)*(s*5.0);
            float d2 = sin(dx*PI);
            yv *= d2*d2;
            col = col*yv;
          return col;
        }
   
   /////////////////////////
    void main ()
    {
            
        vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
        vec4 texColorX = vec4(0.0,0.0,0.0,1.0) * texColor.a;
        
        vec2 image_size = getTexSize(inputImg.size);
        vec2 iRes = vec2(acScreenSize.x, acScreenSize.y);
        
        float maxF = max(acScreenSize.x, acScreenSize.y);
        vec2 texelSize = 1.0 / vec2(maxF, maxF);
        
        vec2 xnxx;
        xnxx = acScreenNorm; 
        
        vec2 nxnn;
        nxnn = ((vec2(acScreenNorm.x,1.0-acScreenNorm.y)*acScreenSize)-acLayerCenter)/acLayerSize;
        nxnn.y *=acLayerSize.y/acLayerSize.x;
        nxnn /= 10.0;
        
        vec2 umefx;
        umefx = ((vec2(acScreenNorm.x,1.0-acScreenNorm.y)*acScreenSize)-acLayerCenter)/acLayerSize;
        umefx.y *=acLayerSize.y/acLayerSize.x;

        umefx /= scale;
        umefx *= rot2m((0.0+angle)*0.0174533);
        umefx += vec2((0.0+position.x), (0.0+position.y))*vec2(-1.,1.)/500.;
        
        vec2 scaleVec;
        scaleVec = vec2(scale*stretch.x,scale*stretch.y);
        umefx /= scaleVec;
        
        if(mirrx) { umefx.x = 1. - umefx.x; }
        if(mirry) { umefx.y = 1. - umefx.y; }
        
        /////////////////////////
        
    	vec4 fragColor;
    	
            float time= autop? PROGRESS+(acTime*0.5) : PROGRESS/10.0;
            // vec2 resolution = umefx;
            vec2 resolution = xnxx;
            float an = autop? sin(PROGRESS+(acTime*0.5)*64564.8) : sin(PROGRESS*64564.8);
        
            vec3 ro = vec3( 16.0*cos(an), 0.0, 16.0*sin(an) );
            vec3 ta = vec3( 0.0, 0.0, 0.0 );
            // camera matrix
            vec3 ww = normalize( ta - ro );
            vec3 uu = normalize( cross(ww,vec3(0.0,1.0,0.0) ) );
            vec3 vv = normalize( cross(uu,ww));
        
            vec3 tot = vec3(0.0);
              // vec2 ppp = xnxx;
              vec2 ppp = umefx;
              // vec2 ppp = (-iRes.xy + 2.*(fragCoord.xy))/iRes.y;
        
                vec3 bbk = spunk(ppp.xy);
          
                // vec2 p = xnxx;
                vec2 p = nxnn;
                // vec2 p = (-iRes.xy + 2.0*fragCoord.xy)/iRes.y;
        
                vec3 rd = normalize( p.x*uu + p.y*vv + 1.5*ww );
                float v = 1.0-abs(p.y);
                vec3 col = bbk*v*2.0 * 3.0; //vec3(v*0.1);
                col = sqrt( col );
               tot += col;
              
          fragColor = vec4( tot, 0.0 );
        
    	vec4 factor = mix(texColorX,fragColor,1.0);

        vec4 resultfinal;
        if( blendMode==0 ) {                // normal
            // resultfinal = (factor * bright) * texColor.a;
            resultfinal = (newLayer(color2, factor) * bright) * texColor.a;
        } else if( blendMode==1 ) {         // multiply
            factor *= bright;
            vec4 c = factor + vec4(1.)*(1.-factor.a);
            resultfinal = vec4((texColor.rgb/texColor.a)*c.rgb,1.0) * texColor.a;
        } else if( blendMode==2 ) {         // screen
            factor *= bright;
            resultfinal = vec4( 1.-((1.-(texColor.rgb/texColor.a))*(1.-factor.rgb)),1.0) * texColor.a;
        }
            
    	gl_FragColor = mix(texColor, resultfinal, alpha);
    	// }
    }


    </shader>

</effect>