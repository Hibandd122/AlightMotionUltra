<?xml version='1.0' encoding='utf-8'?>
<effect category_ultra="Flares, Rays & Energy" id="com.alightcreative.effects.extra.plasma" name="Plasma" desc="Classic animated sine-based plasma field of flowing colors. Can replace the layer or blend with it using normal, multiply or screen modes." category="color" thumb="thumb/1/0plasma.png" tags="procedural,plasma,color,animated,raster">

    <params>
        <texture id="inputImg" srcType="content" />
        <slider id="scale" default="4.0" min="1.0" max="20.0" label="Scale" />
        <slider id="speed" default="1.0" min="0.0" max="5.0" label="Speed" />
        <slider id="alpha" default="1.0" min="0.0" max="1.0" label="Opacity" type="percent" />
        <menu id="blendMode" style="dropdown" default="2" label="Blend Mode">
            <menu label="Normal" value="0" />
            <menu label="Multiply" value="1" />
            <menu label="Screen" value="2" />
        </menu>
    </params>

    <shader type="fragment" precision="high">
        void main(){
            vec4 src = texture2DCv(inputImg.texture, acScreenNorm);
            vec2 uv = acScreenNorm*scale;
            float t = acTime*speed;
            float v = sin(uv.x+t) + sin(uv.y+t) + sin(uv.x+uv.y+t)
                    + sin(length(uv-vec2(scale*0.5))+t);
            v *= 0.25;
            vec3 col = 0.5 + 0.5*cos(6.2831*(v + vec3(0.0,0.33,0.67)));
            vec4 res;
            if(blendMode==1) res = vec4((src.rgb/max(src.a,0.001))*col,1.0)*src.a;
            else if(blendMode==2) res = vec4(1.0-(1.0-src.rgb/max(src.a,0.001))*(1.0-col),1.0)*src.a;
            else res = vec4(col,1.0);
            gl_FragColor = mix(src, res, alpha);
        }
    </shader>

</effect>