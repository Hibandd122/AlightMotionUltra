<?xml version='1.0' encoding='utf-8'?>
<effect category_ultra="Water, Glass & Refraction" id="com.alightcreative.effects.refractionwarp" name="Refraction Warp" desc="Water-like refraction: sums rotated cosine waves into a height field, then displaces the image by its emboss gradient with a specular reflection highlight. Controls cover wave strength, frequency, steps, angle, reflection gain/cutoff/intensity and drift speed." category="distort" tags="distort" thumb="thumb/9/refract.png" experimental="false" overdraw="true" max-overdraw="1.0">

    <params>
        <switch id="autop" default="true" label="Auto Progress" />
        <slider id="PROGRESS" label="Progress" default="1.0" min="-100.0" max="100.0" step="0.01" type="percent" />
        <slider id="SPEEED" label="AutoProg Speed" default="1.0" min="0.0" max="100.0" step="0.01" type="percent" />
        
        <slider id="scale" default="1.0" min="0.1" max="50.0" step="0.01" label="Scale" type="percent" />
        <point id="position" label="@am:string/effect_param_offset" />
        
        <slider id="intensity" label="Strength" default="2.4" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="frequency" label="Frequency" default="6.0" min="0.0" max="100.0" step="0.05" type="percent" />
        <slider id="emboss" label="Emboss" default="0.50" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="steps" label="Steps" default="8" min="0" max="15" step="1.0" type="integer" />
        <slider id="angle" label="Angle" default="7" min="0" max="15" step="1.0" type="integer" />
        
        
        <slider id="delta" label="Delta" default="60.0" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="gain" label="Gain" default="700.0" min="0.0" max="1000.0" step="1" />
        <slider id="reflectionCutOff" label="Cut Off" default="0.012" min="0.0" max="100.0" step="0.001" />
        <slider id="reflectionIntensity" label="Intensity" default="200" min="0.0" max="500.0" step="0.01" />
        
        
        <slider id="speed" label="Octaves" default="0.2" min="0.0" max="10" step="0.01" type="percent" />
        <slider id="speed_x" label="X Speed" default="0.3" min="0.0" max="10" step="0.01" type="percent" />
        <slider id="speed_y" label="Y Speed" default="0.3" min="0.0" max="10" step="0.01" type="percent" />
        
        <slider id="alpha" default="1.0" min="0.0" max="1.0" step="0.01" label="Alpha" type="percent" />
    <texture id="inputImg" srcType="content" />
    <texture id="xcipher" srcType="buffer" downsample="2" />
    </params>
    
    <passes>
        <pass target="xcipher" />
        <pass />
    </passes>
    
    

    <shader type="fragment">
    //////////////////////////
    vec4 newLayer( vec4 a, vec4 b) { return a * (1. - b.a) + b;  }
    mat2 rot2m(float a){ float s = sin(a); float c = cos(a); return mat2(c, -s, s, c); }
    vec2 l2s( vec2 v ) { return (vec4(v,0.,1.)*acLayerToScreen).xy; }
    #define fragCoord gl_FragCoord.xy
    
   /////////////////////////
   
        const float PI = 3.1415926535897932;
        
          float col(vec2 coord,float time)
          {
            float delta_theta = 2.0 * PI / angle;
            float col = 0.0;
            float theta = 0.0;
            for (int i = 0; i &lt; int(steps); i++)
            {
              vec2 adjc = coord;
              theta = delta_theta*float(i);
              adjc.x += cos(theta)*time*speed + time * speed_x;
              adjc.y -= sin(theta)*time*speed - time * speed_y;
              col = col + cos( (adjc.x*cos(theta) - adjc.y*sin(theta))*frequency)*intensity;
            }
        
            return cos(col);
          }
    //////////////////////////
        void main() {
        if( acPass == 0 ) { gl_FragColor = texture2DCv(inputImg.texture, acScreenNorm.st); } else if( acPass==1 ) {
        vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
        vec4 texColorX = vec4(0.0,0.0,0.0,1.0) * texColor.a;
        
        vec2 image_size = getTexSize(inputImg.size);
        vec2 iResolution = vec2(acScreenSize.x, acScreenSize.y);
        vec2 iMouse = vec2(0.5, 0.5);
        float time = autop? PROGRESS + (acTime*SPEEED) : PROGRESS;
        
        vec2 umefx;
        umefx = acScreenNorm.st; 
        umefx /= scale;
        umefx *= rot2m((-180.0)*0.0174533);
        umefx += vec2((0.0+position.x), (0.0-position.y))*vec2(-1.,1.)/500.;
        
        /////////////////////////
        
    	vec4 fragColor;
    	
        vec2 p = vec2(-umefx.x,umefx.y);
        // vec2 p = umefx;
        vec2 c1 = p;
        vec2 c2 = p;
        float cc1 = col(c1,time);
        
        c2.x += iResolution.x/delta;
        float dx = emboss*(cc1-col(c2,time))/delta;
        
        c2.x = p.x;
        c2.y += iResolution.y/delta;
        float dy = emboss*(cc1-col(c2,time))/delta;
        
        c1.x += dx*2.;
        c1.y = -(c1.y+dy*2.);
        
        float alphaa = 1.+dot(dx,dy)*gain;
        	
        float ddx = dx - reflectionCutOff;
        float ddy = dy - reflectionCutOff;
        if (ddx &gt; 0. &amp;&amp; ddy &gt; 0.)
        	alphaa = pow(alpha, ddx*ddy*(reflectionIntensity*1000.0));
        	
        vec4 col = texture2DCv(inputImg.texture,c1)*(alphaa);
        fragColor = col;
        
    	    vec4 factor = mix(texColorX,fragColor,1.0);

    	gl_FragColor = mix(texColor, factor, alpha);
          }
        }

    </shader>

</effect>