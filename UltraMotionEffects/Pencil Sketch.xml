<?xml version='1.0' encoding='utf-8'?>
<effect category_ultra="Ink, Paint & Halftone" id="com.alightcreative.effects.extra2.pencil-sketch" name="Pencil Sketch" desc="Turns the layer into a soft pencil drawing using a classic dodge-blend technique." category="drawing" thumb="thumb/5/hardg.png" tags="stylize,sketch,pencil,artistic,raster" overdraw="true" max-overdraw="1.0">

    <params>
        <texture id="inputImg" srcType="content" />
        <slider id="amount" default="1.0" min="0.0" max="1.0" label="Amount" type="percent" />
    </params>

    <shader type="fragment" precision="high">
        void main(){
            vec2 px = 1.5/acScreenSize;
            vec4 src = texture2DCv(inputImg.texture, acScreenNorm);
            vec3 c = src.a&gt;0.0? src.rgb/src.a : src.rgb;
            float g = dot(c, vec3(0.299,0.587,0.114));
            float b = 0.0;
            b += 1.0 - dot(texture2DCv(inputImg.texture, acScreenNorm+vec2(px.x,0.0)).rgb, vec3(0.333));
            b += 1.0 - dot(texture2DCv(inputImg.texture, acScreenNorm-vec2(px.x,0.0)).rgb, vec3(0.333));
            b += 1.0 - dot(texture2DCv(inputImg.texture, acScreenNorm+vec2(0.0,px.y)).rgb, vec3(0.333));
            b += 1.0 - dot(texture2DCv(inputImg.texture, acScreenNorm-vec2(0.0,px.y)).rgb, vec3(0.333));
            b += 1.0 - g;
            b /= 5.0;
            float dodge = g / (1.0 - b + 0.004);
            dodge = clamp(dodge, 0.0, 1.0);
            gl_FragColor = vec4(mix(c, vec3(dodge), amount),1.0)*src.a;
        }
    </shader>

</effect>