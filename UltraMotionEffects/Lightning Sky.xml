<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.effects.lighting" name="Lightning Sky" desc="Draws procedural branching lightning bolts (segment-marched with noise-driven arcs, warped by fbm) that flash at random intervals. Controls include strike frequency, bolt length, count, ray opacity, color, transform, and normal/multiply/screen blending." category="colorlight" tags="procedural" thumb="thumb/Procedural/Lightning Sky.webp" experimental="false">

    <params>
        <color id="color1" default="#FFFFFFFF" label="Color" alpha="true" />
        <slider id="progressa" label="Progress" default="1.0" min="0" max="1000.0" step="0.01" type="percent" />
        <slider id="ktime" label="Frequency" default="1.1" min="-100.0" max="100.0" step="0.01" type="percent" />
        <slider id="klength" label="Length" default="2.0" min="0" max="100.0" step="0.01" type="percent" />
        <slider id="kalpha" label="Alpha Rays" default="1.0" min="0" max="100.0" step="0.01" type="percent" />
        <slider id="knum" label="Amount" default="2" min="0" max="5" step="1.0" type="integer" ticks="1,2,3,4" snap="1,2,3,4" />
        <slider id="scale" default="1.0" min="0.1" max="50.0" step="0.01" label="Scale" type="percent" />
        <point id="position" label="@am:string/effect_param_offset" />
        <point id="stretch" default="1.0,1.0" min="0.01,0.01" max="10, 10" label="@am:string/effect_param_stretch" step="0.01" />
        <slider id="bright" default="1.0" min="1.0" max="3.0" step="0.01" label="Brightness" type="percent" />
        <slider id="alpha" default="1.0" min="0.0" max="1.0" step="0.01" label="Alpha" type="percent" />
        <slider id="angle" default="0.0" min="-3600" max="3600" ticks="-180,-135,-90,-45,-30,-15,0,15,30,45,90,135,180" label="Rotate" step="0.5" type="angle" />
        <menu id="blendMode" style="radio" default="0" label="@am:string/effect_param_blendmode">
            <menu label="NORMAL" value="0" />
            <menu label="MULTIPLY" value="1" />
            <menu label="SCREEN" value="2" />
        </menu>
        <texture id="inputImg" srcType="content" />

    </params>
    
    <shader type="fragment" precision="high">
    
    vec4 newLayer( vec4 a, vec4 b) {
    return a * (1. - b.a) + b;
    }
    
    mat2 rot2m(float a){
        
	float s = sin(a);
	float c = cos(a);
	
	return mat2(c, -s, s, c);
    }
    
    vec2 l2s( vec2 v ) {
        return (vec4(v,0.,1.)*acLayerToScreen).xy;
    }
    
   #define fragCoord gl_FragCoord.xy
    
   /////////////////////////
   
    float hash(float x)
    {
       return fract(21654.6512 * sin(385.51 * x));
    }
    float hash( in vec2 p ) 
    {
        return fract(sin(p.x*15.32+p.y*35.78) * 43758.23);
    }
    
    vec2 hash2(vec2 p)
    {
       return vec2(hash(p*.754),hash(1.5743*p.yx+4.5891))-.5;
    }
    
    vec2 hash2b(vec2 p)
    {
       return vec2(hash(p*.754),hash(1.5743*p+4.5476351));
    }
    vec2 add = vec2(1.0, 0.0);
    
    vec2 noise2(vec2 x)
    {
        vec2 p = floor(x);
        vec2 f = fract(x);
        f = f*f*(3.0-2.0*f);
        
        vec2 res = mix(mix( hash2(p),          hash2(p + add.xy),f.x),
                        mix( hash2(p + add.yx), hash2(p + add.xx),f.x),f.y);
        return res;
    }
    
    vec2 fbm2(vec2 x)
    {
        vec2 r = vec2(0.0);
        float a = 1.0;
        
        for (int i = 0; i &lt; 8; i++)
        {
            r += abs(noise2(x)+.5 )* a;
            x *= 2.;
            a *= .5;
        }
         
        return r;
    }
    
    mat2 m2;
    
    vec2 fbm3(vec2 x)
    {
        vec2 r = vec2(0.0);
        float a = 1.;
        
        for (int i = 0; i &lt; 6; i++)
        {
            r += m2*noise2((x+r)/a)*a; 
            r=-.8*abs(r);
            a*=1.7;
        }     
        return r;
    }
    
    vec3 storm(vec2 x)
    {
        float t = .5*progressa;
        float st = sin(t), ct = cos(t);
        m2 = mat2(ct,st,-st,ct);
        x=fbm3(x+0.5*progressa)+2.;
        x*=.35;
            
        float c = length(x);
        c=c*c*c;
        vec3 col=vec3(0.6-.1*x.x,0.7,0.8-.1*x.y)*c*x.y;   
        return clamp(col,0.,1.);
    }
    
    
    
    float dseg( vec2 ba, vec2 pa )
    {
       
       float h = clamp( dot(pa,ba)/dot(ba,ba), -0.2, 1. );   
       return length( pa - ba*h );
    }
    
    float arc(vec2 x,vec2 p, vec2 dir)
    {
        vec2 r = p;
        float d=10.;
        for (int i = 0; i &lt; 5; i++)
        {
            vec2 s= noise2(r+progressa)+dir;
            d=min(d,dseg(s,x-r));
            r +=s;      
        }
        return d*3.;
        
    }
    
    
    
    float thunderbolt(vec2 x,vec2 tgt)
    {
        vec2 r = tgt;
        float d=1000.;
        float dist=length(tgt-x);
         
        for (int i = 0; i &lt; 11; i++)
        {
            if(r.y&gt;x.y+.5)break;
            vec2 s= (noise2(r+progressa)+vec2(0.,.7))*2.;
            dist = dseg(s * klength,x-r);
            d=min(d,dist);
            
            r +=s;
            if(i-(i/5)*5==0){
                if(i-(i/10)*10==0)d=min(d,arc(x,r,vec2(.3,.5)));
                else d=min(d,arc(x,r,vec2(-.3,.5)));
            }
        }
    
        return exp(-5.*d)+.2*exp(-1.*dist) ;
       
    }
   
   /////////////////////////
    void main ()
    {
            
        vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
        vec4 texColorX = vec4(0.0,0.0,0.0,1.0) * texColor.a;
        
        vec2 umefx;
        umefx = ((vec2(acScreenNorm.x,1.0-acScreenNorm.y)*acScreenSize)-acLayerCenter)/acLayerSize;
        umefx.y*=acLayerSize.y/acLayerSize.x;

        umefx /= scale;
        umefx *= rot2m((0.0+angle)*0.0174533);
        umefx += vec2((-225.0+position.x), (0.0+position.y))*vec2(-1.,1.)/500.;
        
        vec2 scaleVec;
        scaleVec = vec2(scale*stretch.x,scale*stretch.y);
        umefx /= scaleVec;
        
        /////////////////////////
        
    	vec4 fragColor;
    	
       vec2 p = umefx;
       // vec2 p = 2.*fragCoord.xy/u_resolutiown.yy-1.;
        vec2 d;
        vec2 tgt = vec2(1., -8.);
        float c=0.;
        vec3 col = vec3(0.0);
       
         float t = hash(floor(5.*progressa * ktime));
       
        if(hash(t+2.3) &gt; .8 )
        {
            for(float i = 1.0 ; i &lt;= knum; i++){
    
    
    
                tgt+=8.*hash2b(tgt+t);
                c= thunderbolt(p*10.+2.*fbm2(5.*p),tgt);   
                col+=clamp(1.7* color1.rgb *c,0.,1.);   
    
    
            }
        
        }
        
       fragColor = vec4(col * kalpha, 0.0);
        
    	vec4 factor = mix(texColorX,fragColor,1.0);

        vec4 resultfinal;
        if( blendMode==0 ) {                // normal
            // resultfinal = (factor * bright) * texColor.a;
            resultfinal = (newLayer(texColor, factor) * bright) * texColor.a;
        } else if( blendMode==1 ) {         // multiply
            factor *= bright;
            vec4 c = factor + vec4(1.)*(1.-factor.a);
            resultfinal = vec4((texColor.rgb/texColor.a)*c.rgb,1.0) * texColor.a;
        } else if( blendMode==2 ) {         // screen
            factor *= bright;
            resultfinal = vec4( 1.-((1.-(texColor.rgb/texColor.a))*(1.-factor.rgb)),1.0) * texColor.a;
        }
            
    	gl_FragColor = mix(texColor, resultfinal, alpha);
    	// }
    }


    </shader>

</effect>