<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.effects.extra.frosted-glass" name="Frosted Glass" desc="Soft frosted-glass blur created by randomly scattering samples, with adjustable roughness and a frozen-pattern option." category="blur" thumb="thumb/4/frosted.png" tags="blur,glass,frost,raster" overdraw="true" max-overdraw="1.5">

    <params>
        <texture id="inputImg" srcType="content" />
        <slider id="roughness" default="0.5" min="0.0" max="1.0" label="Roughness" />
        <slider id="samples" default="12" min="4" max="24" label="Quality" step="1" type="integer" />
        <switch id="freeze" default="true" label="Freeze Pattern" />
    </params>

    <shader type="fragment" precision="high">
        float rand(vec2 c){ return fract(sin(dot(c,vec2(12.9898,78.233)))*43758.5453); }
        void main(){
            float t = freeze? 0.0 : acTime;
            vec4 acc = vec4(0.0);
            float n = 0.0;
            float r = roughness*0.04;
            for(int i=0;i&lt;24;i++){
                if(float(i)&gt;=float(samples)) break;
                float a = rand(acScreenNorm*100.0 + float(i) + t)*6.2831;
                float d = rand(acScreenNorm*55.0 + float(i)*1.7 + t)*r;
                vec2 off = vec2(cos(a),sin(a))*d;
                acc += texture2DCv(inputImg.texture, acScreenNorm+off);
                n += 1.0;
            }
            gl_FragColor = acc/n;
        }
    </shader>

</effect>