<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.effects.extra2.duotone" name="Duotone" desc="Maps the layer's brightness onto two colors for a clean two-tone poster look." category="colorlight" thumb="thumb/5/hardg.png" tags="color,duotone,poster,raster">

    <params>
        <texture id="inputImg" srcType="content" />
        <color id="shadow" default="#FF1A0A33" label="Shadow Color" alpha="false" />
        <color id="highlight" default="#FFFFE070" label="Highlight Color" alpha="false" />
        <slider id="amount" default="1.0" min="0.0" max="1.0" label="Amount" type="percent" />
    </params>

    <shader type="fragment" precision="high">
        void main(){
            vec4 src = texture2DCv(inputImg.texture, acScreenNorm);
            vec3 c = src.a&gt;0.0? src.rgb/src.a : src.rgb;
            float l = dot(c, vec3(0.299,0.587,0.114));
            vec3 duo = mix(shadow.rgb, highlight.rgb, l);
            gl_FragColor = vec4(mix(c, duo, amount),1.0)*src.a;
        }
    </shader>

</effect>