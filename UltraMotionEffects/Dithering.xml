<?xml version='1.0' encoding='utf-8'?>
<effect category_ultra="Color Grading" id="com.alightcreative.effects.dith" name="Dithering" desc="Posterizes the layer with a selectable pattern: line or dot sine patterns, or ordered dithering thresholded against a noise texture. Includes a built-in color-correction pre-pass (exposure, gamma, brightness, contrast, highlights, shadows), greyscale/invert switches, and blend modes." category="color" tags="other" thumb="thumb/0/efek.png" experimental="false">

    <params>
        <texture id="noisexx" srcType="image" src="textures/dith.png" />
        <menu id="type" style="radio" default="2" label="Type">
            <menu label="LINE" value="0" />
            <menu label="DOTS" value="1" />
            <menu label="DITHERING" value="2" />
        </menu>
        <slider id="scale" label="Scale" default="1.5" min="0" max="100.0" step="0.01" type="percent" />
        <slider id="amount" label="Amount" default="4.0" min="2.0" max="100.0" step="0.01" type="percent" />
        <slider id="angle" default="20.0" min="-3600" max="3600" label="Angle Texture" step="0.5" type="angle" />
        <slider id="satt" label="Intensify" default="1.2" min="0.5" max="100.0" step="0.01" type="percent" />
        
        <slider id="exposure" default="0.0" min="-2.0" max="2.0" label="@am:string/effect_param_exposure" ticks="-2.0,-1.0,0.0,1.0,2.0" snap="0.0" />
        <slider id="gamma" default="1.0" min="0.01" max="9.99" label="@am:string/effect_param_gamma" logscale="2.351" snap="1.0" ticks="0.5,1.0,2.0" step="0.005" />
        <slider id="brightness" default="0.0" min="-1.0" max="1.0" label="@am:string/effect_param_brightness" ticks="0.0" snap="0.0" step="0.005" type="relative-percent" />
        <slider id="contrast" default="0.0" min="-1.0" max="3.0" label="@am:string/effect_param_contrast" ticks="-1.0,0.0,1.0,3.0" snap="0.0,1.0" step="0.005" type="relative-percent" />
        <slider id="highlights" default="1.0" min="0.0" max="2.0" label="@am:string/effect_param_highlights" snap="1.0" ticks="1.0" step="0.01" type="percent" />
        <slider id="shadows" default="1.0" min="0.0" max="2.0" label="@am:string/effect_param_shadows" snap="1.0" ticks="1.0" step="0.01" type="percent" />
        <slider id="offset" default="0.0" min="-0.9" max="0.9" label="@am:string/effect_param_offset" ticks="0.0" snap="0.0" step="0.01" />
        
        <slider id="alpha" default="1.0" min="0.0" max="1.0" step="0.01" label="Alpha" type="percent" />
        <switch id="greyscale" default="false" label="Greyscale" />
        <switch id="inv" default="false" label="Invert Color" />
        <menu id="blendMode" style="radio" default="0" label="@am:string/effect_param_blendmode">
            <menu label="NORMAL" value="0" />
            <menu label="MULTIPLY" value="1" />
            <menu label="SCREEN" value="2" />
        </menu>
        <texture id="inputImg" srcType="content" />
        <texture id="xdist" srcType="buffer" />

    </params>
        
    <passes>
        <pass target="xdist" effect="com.alightcreative.effects.fastadj" />
        <pass />
    </passes>
    
    <shader type="fragment" precision="high">
    
    vec4 newLayer( vec4 a, vec4 b) {
    return a * (1. - b.a) + b;
    }
    
    mat2 rot2m(float a){
        
	float s = sin(a);
	float c = cos(a);
	
	return mat2(c, -s, s, c);
    }
    
    vec2 l2s( vec2 v ) {
        return (vec4(v,0.,1.)*acLayerToScreen).xy;
    }
    
   #define fragCoord gl_FragCoord.xy
    
   /////////////////////////
   
        float greyScale(in vec3 col) {
            return dot(col, vec3(0.2126, 0.7152, 0.0722));
        }
        
        mat2 rotate2d(float angle){
            return mat2(cos(angle), -sin(angle), sin(angle),cos(angle));
        }
   
   /////////////////////////
    void main ()
    {
            
        vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
        vec4 texColorX = vec4(0.0,0.0,0.0,1.0) * texColor.a;
        
        vec2 image_size = getTexSize(inputImg.size);
        vec2 iRes = vec2(acScreenSize.x, acScreenSize.y);
        
        vec2 umefx;
        umefx = acScreenNorm.st; 
        
        /////////////////////////
        
    	vec4 fragColor;
    	
            
            vec2 uv = umefx;
            // vec2 uv = fragCoord.xy / iResolution.xy;
            vec4 colX = texture2DCv(xdist.texture, uv); 
            // vec3 col = texture2DCv(inputImg.texture, uv).rgb; 
            vec4 col = colX;
            if (greyscale) col.rgb = vec3(greyScale(col.rgb));
            if (greyscale) col.rgb = vec3(1.0) - col.rgb;
            
            uv *= iRes.xy;
            vec2 p = rotate2d(angle) * uv * scale; 
            
            float pattern;
            vec3 xnxx;
            if( type==0 ) {
           	pattern = sin( p.x ) * amount;
            xnxx = col.rgb * 10. * satt - 5. + pattern; }
        
            else if( type==1 ) {
            pattern = sin( p.x ) * sin( p.y ) * amount;
            xnxx = col.rgb * 10. * satt - 5. + pattern; }
            
            else if( type==2 ) {
            // col.rgb = vec3(greyScale(col.rgb));
            pattern = texture2DCv(noisexx.texture, fragCoord / 4. * scale).r*amount/5.;
            xnxx = step(pattern, col.rgb); }
        
            fragColor = vec4( xnxx, 1.0 );
            // fragColor = vec4( xnxx, 1.0 );
        
    	vec4 factor = mix(texColorX,fragColor,1.0);

        vec4 resultfinal;
        if( blendMode==0 ) {                // normal
            resultfinal = (factor * 1.0) * texColor.a;
            // resultfinal = (newLayer(texColor, factor) * 1.0) * texColor.a;
        } else if( blendMode==1 ) {         // multiply
            factor *= 1.0;
            vec4 c = factor + vec4(1.)*(1.-factor.a);
            resultfinal = vec4((texColor.rgb/texColor.a)*c.rgb,1.0) * texColor.a;
        } else if( blendMode==2 ) {         // screen
            factor *= 1.0;
            resultfinal = vec4( 1.-((1.-(texColor.rgb/texColor.a))*(1.-factor.rgb)),1.0) * texColor.a;
        }
            
    	// gl_FragColor = colX;
    	gl_FragColor = mix(texColor, resultfinal, alpha);
    	// }
    }


    </shader>

</effect>