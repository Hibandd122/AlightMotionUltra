<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.effects.lensblurobs" name="Bokeh Lens Blur" desc="Bokeh-style lens blur: samples the image along a golden-angle spiral and weights samples toward bright highlights so defocused highlights bloom into discs. Iterations (10-200) trades quality for speed; Strength and Radius set the blur amount." category="blur" tags="blur, raster" thumb="thumb/Blur and Sharpen/Bokeh Lens Blur.webp" overdraw="true" max-overdraw="1.5" experimental="false">

    <params>
        <texture id="inputImg" srcType="content" />
        
        <slider id="ITERATIONS" label="Iterations" default="100.0" min="10.0" max="200.0" step="1.0" type="integer" ticks="25,50,75,100,125,150,175" snap="25,50,75,100,125,150,175" />
        <slider id="progressa" label="Strength" default="0.0" min="0" max="5.0" step="0.005" type="percent" />
        <slider id="RADIUS" label="Radius" default="1.0" min="1" max="30" step="0.05" type="percent" />
        <slider id="bright" default="1.0" min="1.0" max="3.0" step="0.01" label="Brightness" type="percent" />
        <slider id="alpha" default="1.0" min="0.0" max="1.0" step="0.01" label="Alpha" type="percent" />
        <menu id="blendMode" style="radio" default="0" label="@am:string/effect_param_blendmode">
            <menu label="NORMAL" value="0" />
            <menu label="MULTIPLY" value="1" />
            <menu label="SCREEN" value="2" />
        </menu>

    </params>
    
    <shader type="fragment" precision="high">
    
    mat2 rot2m(float a){
        
	float s = sin(a);
	float c = cos(a);
	
	return mat2(c, -s, s, c);
    }
    
    vec2 l2s( vec2 v ) {
        return (vec4(v,0.,1.)*acLayerToScreen).xy;
    }
    
   #define fragCoord gl_FragCoord
    
   /////////////////////////
   
      #define GOLDEN_ANGLE 2.3999632
      
      mat2 rot = mat2(cos(GOLDEN_ANGLE), sin(GOLDEN_ANGLE), -sin(GOLDEN_ANGLE), cos(GOLDEN_ANGLE));
      
      //-------------------------------------------------------------------------------------------
      vec3 Bokeh(sampler2D tex, vec2 uv, float radius)
      {
          vec3 acc = vec3(0), div = acc;
          float r = RADIUS;
          vec2 vangle = vec2(0.0,radius*.01 / sqrt(float(ITERATIONS)));
          
      	for (float j = 0.0; j &lt; ITERATIONS; j++)
          {  
              r += 1. / r;
      	    vangle = rot * vangle;
              vec3 col = texture2DCv(tex, uv + (r-1.) * vangle).xyz; /// ... Sample the image
      		vec3 bokeh = pow(col, vec3(4));
      		acc += col * bokeh;
      		div += bokeh;
      	}
      	return acc / div;
      }

   
   /////////////////////////
    void main ()
    {
            
        vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
        vec4 texColorX = vec4(0.0,0.0,0.0,1.0) * texColor.a;
        
        vec2 umefx;
        umefx = acScreenNorm; 

    	vec4 fragColor;
    	
          vec2 uv = umefx; //... with correct aspect ratio
          
          
      	float rad = .8 - .8*((progressa+0.15) * 6.283);

          if (0.0 &gt;= 1.0)
          {
          	rad = (0.0/acScreenSize.x)*3.0;
      
          }
          
           fragColor = vec4(Bokeh(inputImg.texture, uv, rad), 1.0);

    	vec4 factor = mix(texColorX,fragColor,alpha);

        vec4 resultfinal;
        if( blendMode==0 ) {                // normal
            resultfinal = (factor * bright) * texColor.a;
        } else if( blendMode==1 ) {         // multiply
            factor *= bright;
            vec4 c = factor + vec4(1.)*(1.-factor.a);
            resultfinal = vec4((texColor.rgb/texColor.a)*c.rgb,1.0) * texColor.a;
        } else if( blendMode==2 ) {         // screen
            factor *= bright;
            resultfinal = vec4( 1.-((1.-(texColor.rgb/texColor.a))*(1.-factor.rgb)),1.0) * texColor.a;
        }
            
    	gl_FragColor = resultfinal;
    	// }
    }


    </shader>

</effect>