<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.effects.differencematte" name="@am:string/effect_diffmatte_name" desc="@am:string/effect_diffmatte_desc" category="mask" thumb="thumb/displacemap.png" experimental="true" deprecated="true">

    <params>
        <texture id="inputImg" srcType="content" />
        <texture id="comp" srcType="comp" />
        <slider id="threshold" default="0.10" min="0.0" max="3.5" step="0.001" label="@am:string/effect_param_threshold" />
        <slider id="feather" default="0.05" min="0.001" max="1.00" step="0.001" label="@am:string/effect_param_feather" />
        <slider id="density" default="0.15" min="0.0" max="1.0" label="@am:string/effect_param_density" />
        <slider id="offset" default="0.01" min="0.0" max="1.0" step="0.001" label="@am:string/effect_param_offset" />
        <menu id="mode" default="0" style="radio" label="@am:string/effect_param_mode">
            <menu label="Alpha" value="0" />
            <menu label="Grayscale" value="1" />
            <menu label="Test" value="2" />
        </menu>
        <menu id="sampling" default="0" style="radio" label="Sampling">
            <menu label="Linear" value="0" />
            <menu label="Smooth" value="1" />
        </menu>

    </params>

    <shader type="fragment" precision="high">

        void main() {
            vec2 texelSize = 1.0 / getTexSize(inputImg.size);
            //vec4 fg = texture2DCv(inputImg.texture,acScreenNorm);
            //vec4 bg = texture2DCv(comp.texture,acScreenNorm);
            //float diff = length(fg.rgb - bg.rgb) * fg.a;

            float lowCutoff = threshold-(threshold*feather);
            float hiCutoff = threshold;
            float range = hiCutoff - lowCutoff;

            float pLeft = 0.0;
            float pRight = 0.0;
            float pUp = 0.0;
            float pDown = 0.0;


            vec2 velocity;

            // ------- HORIZONTAL ---------------------------------------------------------------
            velocity = vec2(0.1,0.0) * density;
            velocity *= acPreviewSize*texelSize;
            velocity.x *= acScreenSize.y / acScreenSize.x;
            float speed = length(velocity / texelSize);
            int nSamples = int(clamp(speed, 1.01, 30.01));

            // RIGHT
            for(int i=1; i&lt;nSamples; i++ ) {
                vec2 offset = velocity * (float(i) / float(nSamples - 1) /*- 0.5*/ );
                vec2 sampleCoord;

                sampleCoord = acScreenNorm.st+offset;
                float insideTexture = step(0.0,sampleCoord.x) * step(sampleCoord.x,1.0) * step(0.0,sampleCoord.y) * step(sampleCoord.y,1.0);
                //vec4 samp = texture2DCv(inputImg.texture, sampleCoord) * insideTexture;

                vec4 fg = texture2DCv(inputImg.texture,sampleCoord) * insideTexture;;
                vec4 bg = texture2DCv(comp.texture,sampleCoord);
                float diff = length(fg.rgb - bg.rgb) * fg.a;

                float t;
                if( sampling == 0 ) {
                    t = clamp((diff - lowCutoff)/range,0.0,1.0);
                } else {
                    t = smoothstep(lowCutoff,hiCutoff,diff);
                }

                pRight = max(pRight, t);

            }


            // LEFT
            for(int i=1; i&lt;nSamples; i++ ) {
                vec2 offset = velocity * (float(i) / float(nSamples - 1) /*- 0.5*/ );
                vec2 sampleCoord;

                sampleCoord = acScreenNorm.st-offset;
                float insideTexture = step(0.0,sampleCoord.x) * step(sampleCoord.x,1.0) * step(0.0,sampleCoord.y) * step(sampleCoord.y,1.0);
                //vec4 samp = texture2DCv(inputImg.texture, sampleCoord) * insideTexture;

                vec4 fg = texture2DCv(inputImg.texture,sampleCoord) * insideTexture;;
                vec4 bg = texture2DCv(comp.texture,sampleCoord);
                float diff = length(fg.rgb - bg.rgb) * fg.a;

                float t;
                if( sampling == 0 ) {
                    t = clamp((diff - lowCutoff)/range,0.0,1.0);
                } else {
                    t = smoothstep(lowCutoff,hiCutoff,diff);
                }

                pLeft = max(pLeft, t);

            }

            // ------- VERTICAL ---------------------------------------------------------------
            velocity = vec2(0.0,0.1) * density;
            velocity *= acPreviewSize * texelSize;
            velocity.x *= acScreenSize.y / acScreenSize.x;
            speed = length(velocity / texelSize);
            nSamples = int(clamp(speed, 1.01, 30.01));

            // UP
            for(int i=1; i&lt;nSamples; i++ ) {
                vec2 offset = velocity * (float(i) / float(nSamples - 1) /*- 0.5*/ );
                vec2 sampleCoord;

                sampleCoord = acScreenNorm.st+offset;
                float insideTexture = step(0.0,sampleCoord.x) * step(sampleCoord.x,1.0) * step(0.0,sampleCoord.y) * step(sampleCoord.y,1.0);
                //vec4 samp = texture2DCv(inputImg.texture, sampleCoord) * insideTexture;

                vec4 fg = texture2DCv(inputImg.texture,sampleCoord) * insideTexture;;
                vec4 bg = texture2DCv(comp.texture,sampleCoord);
                float diff = length(fg.rgb - bg.rgb) * fg.a;

                float t;
                if( sampling == 0 ) {
                    t = clamp((diff - lowCutoff)/range,0.0,1.0);
                } else {
                    t = smoothstep(lowCutoff,hiCutoff,diff);
                }

                pUp = max(pUp, t);

            }


            // DOWN
            for(int i=1; i&lt;nSamples; i++ ) {
                vec2 offset = velocity * (float(i) / float(nSamples - 1) /*- 0.5*/ );
                vec2 sampleCoord;

                sampleCoord = acScreenNorm.st-offset;
                float insideTexture = step(0.0,sampleCoord.x) * step(sampleCoord.x,1.0) * step(0.0,sampleCoord.y) * step(sampleCoord.y,1.0);
                //vec4 samp = texture2DCv(inputImg.texture, sampleCoord) * insideTexture;

                vec4 fg = texture2DCv(inputImg.texture,sampleCoord) * insideTexture;;
                vec4 bg = texture2DCv(comp.texture,sampleCoord);
                float diff = length(fg.rgb - bg.rgb) * fg.a;

                float t;
                if( sampling == 0 ) {
                    t = clamp((diff - lowCutoff)/range,0.0,1.0);
                } else {
                    t = smoothstep(lowCutoff,hiCutoff,diff);
                }

                pDown = max(pDown, t);

            }

            float p = max(min(pLeft,pRight), min(pUp,pDown));

            vec4 fg = texture2DCv(inputImg.texture,acScreenNorm);
            if( density &lt; 0.0001 ) {
                vec4 bg = texture2DCv(comp.texture,acScreenNorm);
                float diff = length(fg.rgb - bg.rgb) * fg.a;
                if( sampling == 0 ) {
                    p = clamp((diff - lowCutoff)/range,0.0,1.0);
                } else {
                    p = smoothstep(lowCutoff,hiCutoff,diff);
                }
            }

            if( p &lt; offset ) p = 0.0;

            if( mode==0 ) {
                gl_FragColor = fg * p;
            } else if( mode==1 ) {
                gl_FragColor = vec4(p,p,p,1.0) * fg.a;
            } else if( mode==2 ) {
                gl_FragColor = vec4(pLeft,pUp,pRight,1.0) * fg.a;
            } else {
                gl_FragColor = vec4(p,p,p,1.0) * fg.a;
            }
        }

            //float p = smoothstep(threshold-(threshold*feather),threshold,diff);


    </shader>

</effect>