<?xml version='1.0' encoding='utf-8'?>
<effect category_ultra="Grids & Tunnels" id="com.alightcreative.effects.2dtunnel" name="2D Tunnel" desc="Generates an animated 2D tunnel of concentric sine-wave bands in polar coordinates, mixed between a background and line color. Controls include Evolution, Amplitude, Hole Depth, scale, offset, angle, brightness, and normal/multiply/screen blending." category="repeat" tags="other" thumb="thumb/3/tunnel.png" experimental="false">

    <params>
        <texture id="inputImg" srcType="content" />
        <texture id="comp" srcType="comp" />
        <color id="color1" default="#00000000" label="Color BG" alpha="true" />
        <color id="color2" default="#FF00FFFF" label="Color Line" alpha="true" />
        <switch id="scalex" default="false" label="Scale Normalize" />
        
        <slider id="scale" default="1.0" min="1.0" max="20.0" step="0.005" label="Scale" type="percent" />
        <slider id="progressa" label="Evolution" default="1.0" min="-100.0" max="100.0" step="0.005" type="percent" />
        <slider id="progressb" label="Amplitude" default="1.0" min="1.0" max="10.0" step="0.125" type="percent" />
        <slider id="progressc" label="Hole Depth" default="0.0" min="0" max="1000.0" step="2.0" type="percent" />
        <point id="position" label="@am:string/effect_param_offset" />
        <point id="stretch" default="1.0,1.0" min="0.01,0.01" max="10, 10" label="@am:string/effect_param_stretch" step="0.01" />
        <slider id="bright" default="1.0" min="1.0" max="10.0" step="0.01" label="Brightness" type="percent" />
        <slider id="alpha" default="1.0" min="0.0" max="1.0" step="0.01" label="Alpha" type="percent" />
        <slider id="angle" default="0.0" min="-3600" max="3600" ticks="-180,-135,-90,-45,-30,-15,0,15,30,45,90,135,180" label="Angle" step="0.5" type="angle" />
        <menu id="blendMode" style="radio" default="0" label="@am:string/effect_param_blendmode">
            <menu label="NORMAL" value="0" />
            <menu label="MULTIPLY" value="1" />
            <menu label="SCREEN" value="2" />
        </menu>

    </params>
    
    <shader type="fragment" precision="high">
    
    vec4 newLayer( vec4 a, vec4 b) {
    return a * (1. - b.a) + b;
    }
    
    mat2 rot2m(float a){
        
	float s = sin(a);
	float c = cos(a);
	
	return mat2(c, -s, s, c);
    }
    
    vec2 l2s( vec2 v ) {
        return (vec4(v,0.,1.)*acLayerToScreen).xy;
    }
    
   #define fragCoord gl_FragCoord.xy
    
   /////////////////////////
   
   #define PI 3.14159
   
   /////////////////////////
    void main ()
    {
            
        vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
        vec4 texColorX = vec4(0.0,0.0,0.0,1.0) * texColor.a;
        
        vec2 umefx;
        umefx = ((vec2(acScreenNorm.x,1.0-acScreenNorm.y)*acScreenSize)-acLayerCenter)/acLayerSize;
        umefx.y*=acLayerSize.y/acLayerSize.x;

        umefx /= scalex? (scale+0.7) : (scale-0.82);
        umefx *= rot2m((0.0+angle)*0.0174533);
        umefx += vec2((0.0+position.x), (0.0+position.y))*vec2(-1.,1.)/500.;
        
        vec2 scaleVec;
        scaleVec = vec2(scale*stretch.x,scale*stretch.y);
        umefx /= scaleVec;
        
        /////////////////////////
        
    	vec4 fragColor;
    	
            vec2 center = umefx;
            center.x *= acScreenSize.x / acScreenSize.x;
                       
            vec4 lineT;
            float a = 1. / length(center) + atan(center.x, center.y) / PI * progressb + progressa;
            float b = sin(a * PI * 8.) * dot(center, center) * acScreenSize.y / (progressc + 300.0) - 0.3;
            float c = b * sin(length(center) - .1);

            lineT = mix(color1, color2, c);
            
            fragColor = lineT;
            // fragColor = newLayer(color3, lineT) * texColor.a;
        
    	vec4 factor = mix(texColorX,fragColor,1.0);

        vec4 resultfinal;
        if( blendMode==0 ) {                // normal
            resultfinal = (factor * bright) * texColor.a;
        } else if( blendMode==1 ) {         // multiply
            factor *= bright;
            vec4 c = factor + vec4(1.)*(1.-factor.a);
            resultfinal = vec4((texColor.rgb/texColor.a)*c.rgb,1.0) * texColor.a;
        } else if( blendMode==2 ) {         // screen
            factor *= bright;
            resultfinal = vec4( 1.-((1.-(texColor.rgb/texColor.a))*(1.-factor.rgb)),1.0) * texColor.a;
        }
            
    	gl_FragColor = mix(texColor, resultfinal, alpha);
    	// }
    }


    </shader>

</effect>