<?xml version='1.0' encoding='utf-8'?>
<effect category_ultra="Glow & Gradients" id="com.alightcreative.effects.burningwipe" name="Burning Wipe+" desc="Burning-paper wipe transition: an fbm-noise front sweeps across the layer, charring it, adding a glowing ember edge in the chosen color, then revealing a solid background color (key it out with Chroma Key for transparency). Controls include progress, threshold, noise evolution, seed, and transform." category="color" tags="transition" thumb="thumb/1/0burning.png" experimental="false">

    <params>
        <texture id="inputImg" srcType="content" />
        <texture id="comp" srcType="comp" />
        <color id="color" default="#FFC53A1B" label="@am:string/effect_param_color" alpha="true" />
        <color id="colorbg" default="#FF5BAA5B" label="Color BG" alpha="true" />
        <slider id="progressa" label="Progress" default="1.0" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="deltab" label="Thresold" default="0.5" min="-5.0" max="5.0" step="0.01" type="relative-percent" />
        <slider id="deltac" label="Evolution" default="15.1" min="-5.0" max="50.0" step="0.01" type="percent" />
        <slider id="deltad" label="Seed" default="0.0" min="-15.0" max="15.0" step="0.1" />
        <slider id="scale" default="1.0" min="0.1" max="50.0" step="0.01" label="Scale" type="percent" />
        <point id="position" label="@am:string/effect_param_offset" />
        <point id="stretch" default="1.0,1.0" min="0.01,0.01" max="10, 10" label="@am:string/effect_param_stretch" step="0.01" />
        <slider id="alpha" default="1.0" min="0.0" max="1.0" step="0.01" label="Alpha" type="percent" />
        <slider id="angle" default="0.0" min="-3600" max="3600" ticks="-180,-135,-90,-45,-30,-15,0,15,30,45,90,135,180" label="Rotate" step="0.5" type="angle" />

    </params>
        
    <shader type="fragment" precision="high">
    
    vec4 newLayer( vec4 a, vec4 b) {
    return a * (1. - b.a) + b;
    }
    
    mat2 rot2m(float a){
        
	float s = sin(a);
	float c = cos(a);
	
	return mat2(c, -s, s, c);
    }
    
    vec2 l2s( vec2 v ) {
        return (vec4(v,0.,1.)*acLayerToScreen).xy;
    }
    
   #define fragCoord gl_FragCoord.xy
    
   /////////////////////////
   
        float Hash( vec2 p)
        {
        	vec3 p2 = vec3(p.xy,deltad);
            return fract(sin(dot(p2,vec3(37.1,61.7, 12.4)))*3758.5453123);
        }
        
        float noise(in vec2 p)
        {
            vec2 i = floor(p);
        	vec2 f = fract(p);
        	f *= f * (3.0-2.0*f);
        
            return mix(mix(Hash(i + vec2(0.,0.)), Hash(i + vec2(1.,0.)),f.x),
        		mix(Hash(i + vec2(0.,1.)), Hash(i + vec2(1.,1.)),f.x),
        		f.y);
        }
        
        float fbm(vec2 p) 
        {
        	float v = 0.0;
        	v += noise(p*1.)*.5;
        	v += noise(p*2.)*.25;
        	v += noise(p*4.)*.125;
        	return v;
        }
   
   /////////////////////////
    void main ()
    {
            
        vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
        
        vec2 umefx;
        umefx = ((vec2(acScreenNorm.x,1.0-acScreenNorm.y)*acScreenSize)-acLayerCenter)/acLayerSize;
        umefx.y*=acLayerSize.y/acLayerSize.x;

        umefx /= scale;
        umefx *= rot2m((70.0+angle)*0.0174533);
        umefx += vec2((520.0+position.x), (0.0+position.y))*vec2(-1.,1.)/500.;
        
        vec2 scaleVec;
        scaleVec = vec2(scale*stretch.x,scale*stretch.y);
        umefx /= scaleVec;
        
        /////////////////////////
        
    	vec4 fragColor;
    	
                vec2 uv = umefx;
        	
        	vec3 src = texture2DCv(inputImg.texture, acScreenNorm).rgb;
            
        	vec4 tgt = vec4(colorbg.rgb, 0.0);
        	
        	vec4 col = vec4(src, 1.0);
        	
        	float ctime = mod(progressa*.5,2.5);
        	
        	float d = uv.x+uv.y*0.5 + deltab*fbm(uv*deltac) + ctime*1.3;
        	if (d &gt;0.35) col.rgb = clamp(col.rgb-(d-0.35)*10.,0.0,1.0);
        	if (d &gt;0.47) {
        	if (d &lt; 0.5 ) col.rgb += (d-0.4)*33.0*0.5*(0.0+noise(100.*uv+vec2(-ctime*2.,0.)))*vec3(color.rgb);
        	else col += tgt; }
        	
        	fragColor = col * texColor.a;
                    
         	gl_FragColor = mix(texColor, fragColor, alpha);
    	
    	
    }


    </shader>

</effect>