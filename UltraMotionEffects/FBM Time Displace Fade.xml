<?xml version='1.0' encoding='utf-8'?>
<effect category_ultra="Wipes, Keys & Shatter" id="custom.fbm_time_displace_fade" name="FBM Time Displace Fade" desc="Displaces the image horizontally and vertically using FBM noise scaled by a cyclic manual time value, then applies a procedural noise-based fade mask. Controls include time, time divisor, FBM scale, displacement clamp and fade bias." category="matte" tags="extra" thumb="thumb/turbulent_displace.webp" experimental="false" overdraw="true" max-overdraw="1.0">

    <params>
        <texture id="inputImg" srcType="content" label="Image" />

        
        <slider id="time" default="0.0" min="0.0" max="10000.0" label="Time" step="0.001" />
        <slider id="timeDiv" default="2.0" min="0.1" max="10.0" label="Time Divisor (fract iTime/Div)" step="0.01" />
        <slider id="timeScale" default="3.0" min="0.1" max="10.0" label="Secondary Time Scale (iTime/Scale)" step="0.01" />

        
        <slider id="fbmScale" default="50.0" min="1.0" max="500.0" label="FBM Pixel Scale" step="0.1" />
        <slider id="dispClamp" default="0.3333" min="0.0" max="2.0" label="Displacement Clamp" step="0.001" />

        
        <slider id="fadeBias" default="1.0" min="0.0" max="3.0" label="Fade Bias" step="0.01" />
    </params>

    <shader type="fragment" precision="high">
        // Port of provided ShaderToy-style effect (manual time control)
        // Original concepts: fbm noise, displacement, time-based fade

        float random(vec2 _st){
            return fract(sin(dot(_st.xy, vec2(12.9898,78.233))) * 43758.5453123);
        }
        float noise2d(vec2 _st){
            vec2 i = floor(_st);
            vec2 f = fract(_st);
            float a = random(i);
            float b = random(i + vec2(1.0, 0.0));
            float c = random(i + vec2(0.0, 1.0));
            float d = random(i + vec2(1.0, 1.0));
            vec2 u = f * f * (3.0 - 2.0 * f);
            return mix(a, b, u.x) + (c - a) * u.y * (1.0 - u.x) + (d - b) * u.x * u.y;
        }
        #define NUM_OCTAVES 5
        float fbm(vec2 _st){
            float v = 0.0;
            float a = 0.5;
            vec2 shift = vec2(100.0);
            mat2 rot = mat2(cos(0.5), sin(0.5), -sin(0.5), cos(0.50));
            for (int i = 0; i &lt; NUM_OCTAVES; ++i) {
                v += a * noise2d(_st);
                _st = rot * _st * 2.0 + shift;
                a *= 0.5;
            }
            return v;
        }
        float workingPow3(float x){ return x * x * x; }

        void main(){
            vec2 res = acScreenSize;
            vec2 fragCoord = acScreenNorm * res;
            vec2 uv = fragCoord / res; // 0..1

            // Time mapping from params (no auto acTime)
            float modifiedTime = workingPow3(fract(time / max(timeDiv, 0.0001)) - 0.5) * 4.0;

            // Secondary time scale (iTime/3. in source) kept for parity if needed
            float timeB = time / max(timeScale, 0.0001);
            (void)timeB; // not used directly but preserved for parity

            // FBM-based displacements
            float verticalDisp   = clamp(fbm(fragCoord / fbmScale)    * modifiedTime, -1.0, 1.0) / max(dispClamp, 0.0001);
            float horizontalDisp = clamp(fbm(fragCoord.yx / fbmScale) * modifiedTime, -1.0, 1.0) / max(dispClamp, 0.0001);

            vec2 offsetCoords = uv - vec2(horizontalDisp, verticalDisp);
            vec4 tex = texture2DCv(inputImg.texture, offsetCoords);

            // Fade mask (procedural, similar structure to source)
            float mask = clamp(tex.a * (1.0 - (abs(modifiedTime) * 2.0 + fbm(uv * 3.0))) * fadeBias, 0.0, 1.0);

            vec3 outRGB = mix(vec3(0.0), tex.rgb, mask);
            gl_FragColor = vec4(outRGB, 1.0);
        }
    </shader>
</effect>