<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.effects.spinblur" name="@string/name" desc="@string/desc" category="blur" thumb="thumb/spin_blur.webp" deprecated="true">

    

    <params>
        <texture id="inputImg" srcType="content" />
        <slider id="angle" default="5" min="0.0" max="60.0" label="@string/angle" step="0.01" type="angle" />
        
        <slider id="cx" default="0.5" min="0.0" max="1.0" label="X" ticks="0.5" snap="0.5" />
        <slider id="cy" default="0.5" min="0.0" max="1.0" label="Y" ticks="0.5" snap="0.5" />
    </params>

    <shader type="fragment">

        void main() {
            vec2 texelSize = 1.0 / getTexSize(inputImg.size);
            vec2 center = (vec4(cx,cy,0.0,1.0) * acLayerToScreen).xy;
            vec2 v = acScreenNorm.st - center;
            float r = length(v);
            float d = 6.283185*r;
            float l = d*angle/360.0;
            float a = angle * 0.0174533;
            float amin = a/2.0;
            float speed = l/texelSize.x;
            int nSamples = int(clamp(speed, 1.01, 100.01));
            vec4 outColor = texture2DCv(inputImg.texture,acScreenNorm.st);
            for(int i=1; i&lt;nSamples; i++ ) {
                float sampleAngle = -amin + a * float(i)/float(nSamples);
                float s = sin(sampleAngle);
                float c = cos(sampleAngle);
                mat2 rot = mat2(c, -s, s, c);
                outColor += texture2DCv(inputImg.texture, center + v*rot);
            }
            gl_FragColor = outColor/float(nSamples);
        }

    </shader>

</effect>