<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.effects.glowribbon" name="Glow Ribbon+" desc="Draws a bundle of glowing sine-wave ribbons whose count, wave progress and evolution are adjustable. Key color, hue shift, luminance, scale, stretch, rotation and normal/multiply/screen blending shape the final look." category="colorlight" tags="procedural" thumb="thumb/1/0glowr.png" experimental="false">

    <params>
        <texture id="inputImg" srcType="content" />
        <texture id="black" srcType="image" src="textures/black.png" />
        <color id="colorkey" default="#FFFFFFFF" label="Key Color" alpha="true" />
        <slider id="AMOUNT" label="Amount" default="5.0" min="1" max="30" step="1.0" type="integer" ticks="5,10,15,20,25" snap="5,10,15,20,25" />
        <slider id="progressa" label="Progress" default="50" min="0" max="1000.0" step="0.05" type="percent" />
        <slider id="progressb" label="Evolution" default="50" min="0" max="1000.0" step="0.01" type="percent" />
        <slider id="scale" default="0.5" min="0.1" max="50.0" step="0.01" label="Scale" type="percent" />
        <slider id="luminance" label="Luminance" default="2.0" min="0.5" max="5.0" step="0.05" type="percent" />
        <point id="position" label="@am:string/effect_param_offset" />
        <point id="stretch" default="1.0,1.0" min="0.01,0.01" max="10, 10" label="@am:string/effect_param_stretch" step="0.01" />
        <slider id="angle" default="0.0" min="-3600" max="3600" ticks="-180,-135,-90,-45,-30,-15,0,15,30,45,90,135,180" label="Rotate" step="0.5" type="angle" />
        <slider id="alpha" default="1.0" min="0.0" max="1.0" step="0.01" label="Alpha" type="percent" />
        <slider id="hue" default="0.0" min="0.0" max="3600.0" label="@am:string/effect_param_hueshift" ticks="0,60,120,180,240,300,360" snap="60,120,180,240,300" type="angle" step="0.5" />
        <menu id="blendMode" style="radio" default="2" label="@am:string/effect_param_blendmode">
            <menu label="NORMAL" value="0" />
            <menu label="MULTIPLY" value="1" />
            <menu label="SCREEN" value="2" />
        </menu>
    </params>
    
    <shader type="fragment" precision="high">
    
    mat2 rot2m(float a){
        
	float s = sin(a);
	float c = cos(a);
	
	return mat2(c, -s, s, c);
    }

    vec2 l2s( vec2 v ) {
        return (vec4(v,0.,1.)*acLayerToScreen).xy;
    }
    
            
   /////////////////////////
    
    float squared(float value) { return value * value; }
    
    float getAmp(float frequency) { return texture2DCv(black.texture, vec2(frequency / 512.0, 0)).x; }
    
    float getWeight(float f) {
        return (+ getAmp(f-2.0) + getAmp(f-1.0) + getAmp(f+2.0) + getAmp(f+1.0) + getAmp(f)) / 5.0; }
    
    void main ()
    {
        vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
            
        highp mat3 rgb2yuv = mat3(
                 0.299,     0.587,    0.114,
                -0.14713,  -0.28886,  0.436,
                 0.615,    -0.51499, -0.10001
            );
            highp mat3 yuv2rgb = mat3(
                1.0,  0.0,      1.13983,
                1.0, -0.39465, -0.58060,
                1.0,  2.03211,  0.0
            );
            float rad = hue * 0.0174533;
            highp mat3 huemat = mat3(
                1.0,  0.0,       0.0,
                0.0,  cos(rad),  sin(rad),
                0.0,  -sin(rad), cos(rad)
            );
                
        /////////////////////////
        
    	vec4 result;
    	
    	// vec2 uvTrue = sampleCoord;
    	
    	vec2 uvTrue;
        uvTrue = ((vec2(acScreenNorm.x,1.0-acScreenNorm.y)*acScreenSize)-acLayerCenter)/acLayerSize;
        uvTrue.y *=acLayerSize.y/acLayerSize.x;
    	
        vec2 uv = uvTrue;
        // vec2 uv = -1.0 + 2.0 * uvTrue;
        uv /= scale;
        uv += position*vec2(-1.,1.)/500.;
        uv.x *= acLayerSize.x/acLayerSize.y;
        uv *= rot2m(angle*0.0174533);
        
        vec2 scaleVec;
        scaleVec = vec2(scale*stretch.x,scale*stretch.y);
        uv /= scaleVec;
        
    	float lineIntensity;
        float glowWidth;
        vec3 color = vec3(0.0);
        
    	for(float i = 0.0; i &lt; AMOUNT; i++) {
            
    		uv.y += (0.2 * sin(uv.x + i/7.0 - progressa * 0.6));
            float Y = uv.y + getWeight(squared(i) * 20.0) *
                (texture2DCv(black.texture, vec2(uvTrue.x, 1)).x - 0.5);
            // lineIntensity = 0.4 + squared(1.6 * abs(mod(uvTrue.x + i / 1.3 + progressb + 2.0) - 1.0)) + (offset * vec2(-1.,1.))/1000.0;
            lineIntensity = 0.4 + squared(1.6 * abs(mod(uvTrue.x + i / 1.3 + progressb,2.0) - 1.0));
    		glowWidth = abs(lineIntensity / (150.0 * Y));
    		color += vec3(glowWidth * (luminance + sin(2.0 * 1.0)), glowWidth * (luminance - sin(0.0 * 0.6)), glowWidth * (luminance - cos(0.0 * 1.0)));
    	}	
    	
    	result = vec4(color, 1.0);
    	result *= 1.0;
    	                	
            vec4 huecolor;
    	    highp vec3 rgb = result.rgb;
            lowp float a = result.a;
            highp vec3 yuv = rgb * rgb2yuv;
            
            float origSat = min(1.0,length(yuv.yz)*2.5); //
            vec2 satUV = yuv.yz * 1.0;
            yuv.yz = mix(satUV,yuv.yz,origSat);
            
            yuv.yz = yuv.yz * 0.0;
            rgb = yuv * yuv2rgb;
            huecolor = vec4(rgb,a);
            
    	    huecolor.rgb *= colorkey.rgb;
    	    huecolor.rgb *= huemat;
            // yuv = yuv * huemat;

    	
        vec4 factor = mix(texColor,huecolor, alpha);
        vec4 resultfinal;
        if( blendMode==0 ) {                // normal
            factor *= texColor.a;
            resultfinal = factor + (texColor*(1.0-factor.a));
        } else if( blendMode==1 ) {         // multiply
            vec4 c = factor + vec4(1.)*(1.-factor.a);
            resultfinal = vec4((texColor.rgb/texColor.a)*c.rgb,1.0) * texColor.a;
        } else if( blendMode==2 ) {         // screen
            resultfinal = vec4( 1.-((1.-(texColor.rgb/texColor.a))*(1.-factor.rgb)),1.0) * texColor.a;
        }
            
    	gl_FragColor = resultfinal;
    }


    </shader>

</effect>