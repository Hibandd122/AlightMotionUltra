<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.effects.dotsloader" name="Dots Spinner" desc="Draws a ring of orbiting glowing dots: each dot position comes from sine/cosine offsets (Orient and Space X/Y) and is rendered as an inverse-distance glow in the chosen color. Amount sets the dot count." category="procedural" tags="other" thumb="thumb/Procedural/Dots Spinner.webp" experimental="false">

    <params>
        <color id="COLOR" default="#FF00FFFF" label="Color" alpha="true" />
        <slider id="AMOUNT" label="Amount" default="8" min="0" max="40" step="1.0" type="integer" snap="5,10,15" />
        <slider id="xxc" label="Radius Glow" default="0.01" min="0" max="0.02" step="0.0005" type="percent" />
        <slider id="xxb" label="Orient X" default="1.0" min="-100.0" max="100.0" step="0.01" type="percent" ticks="0.0" snap="0.0" />
        <slider id="xxa" label="Orient Y" default="1.0" min="-100.0" max="100.0" step="0.01" type="percent" ticks="0.0" snap="0.0" />
        <slider id="xxe" label="Space X" default="0.2" min="-100.0" max="100.0" step="0.01" type="percent" ticks="0.0" snap="0.0" />
        <slider id="xxd" label="Space Y" default="0.2" min="-100.0" max="100.0" step="0.01" type="percent" ticks="0.0" snap="0.0" />
        
        <slider id="scale" default="1.0" min="0.1" max="50.0" step="0.01" label="Scale" type="percent" />
        <slider id="angle" default="0.0" min="-3600" max="3600" ticks="-180,-135,-90,-45,-30,-15,0,15,30,45,90,135,180" label="Rotate" step="0.5" type="angle" />
        <point id="position" label="@am:string/effect_param_offset" />
        <point id="stretch" default="1.0,1.0" min="0.01,0.01" max="10, 10" label="@am:string/effect_param_stretch" step="0.01" />
        <slider id="alpha" default="1.0" min="0.0" max="1.0" step="0.01" label="Alpha" type="percent" />
        <slider id="hardness" default="1.0" min="1.0" max="3.0" step="0.01" label="Hard/Brightness" type="percent" />
        <menu id="blendMode" style="radio" default="0" label="Blending">
            <menu label="NORMAL" value="0" />
            <menu label="MULTIPLY" value="1" />
            <menu label="SCREEN" value="2" />
        </menu>
    <texture id="inputImg" srcType="content" />
    <texture id="xcipher" srcType="buffer" downsample="2" />
    </params>
    
    <passes>
        <pass target="xcipher" />
        <pass />
    </passes>
    
    

    <shader type="fragment">
    //////////////////////////
    vec4 newLayer( vec4 a, vec4 b) { return a * (1. - b.a) + b;  }
    mat2 rot2m(float a){ float s = sin(a); float c = cos(a); return mat2(c, -s, s, c); }
    vec2 l2s( vec2 v ) { return (vec4(v,0.,1.)*acLayerToScreen).xy; }
    #define fragCoord gl_FragCoord.xy
    //////////////////////////
        void main() {
        if( acPass == 0 ) { gl_FragColor = texture2DCv(inputImg.texture, acScreenNorm.st); } else if( acPass==1 ) {
        vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
        vec4 texColorX = vec4(0.0,0.0,0.0,1.0) * texColor.a;
        
        vec2 image_size = getTexSize(inputImg.size);
        vec2 iResolution = vec2(acScreenSize.x, acScreenSize.y);
        vec2 iMouse = vec2(0.5, 0.5);
        
        vec2 umefx;
        umefx = ((vec2(acScreenNorm.x,1.0-acScreenNorm.y)*acScreenSize)-acLayerCenter)/acLayerSize;
        umefx.y*=acLayerSize.y/acLayerSize.x;
        umefx /= scale;
        umefx *= rot2m((0.0+angle)*0.0174533);
        umefx += vec2((0.0+position.x), (0.0+position.y))*vec2(-1.,1.)/500.;
        vec2 scaleVec;
        scaleVec = vec2(scale*stretch.x,scale*stretch.y);
        umefx /= scaleVec;
        
        /////////////////////////
        
    	vec4 fragColor;
    	
            vec2 p = umefx;
        
            float f = 0.0;
            
            for(float i = 1.0; i &lt;= AMOUNT; i++)
            {
                float s = sin(0.7 * xxa + (i * 0.5) * xxa) * xxd;
                float c = cos(0.2 * xxb + (i * 0.5) * xxb) * xxe ;
                f += xxc / abs(length(p + vec2(c, s)));
                // f += 0.01 / abs(length(p*0.5 + vec2(c, s)));
            }
            
        	fragColor = vec4(COLOR.rgb*f, 1.0);
        
    	vec4 factor = mix(texColorX,fragColor,1.0);
    	
        vec4 resultfinal;
          if( blendMode==0 ) {
            resultfinal = (factor * hardness) * texColor.a; }
            // resultfinal = (newLayer(texColor, factor) * hardness) * texColor.a; }
          else if( blendMode==1 ) { factor *= hardness;
            vec4 c = factor + vec4(1.)*(1.-factor.a);
            resultfinal = vec4((texColor.rgb/texColor.a)*c.rgb,1.0) * texColor.a; }
            // resultfinal = min(factor, texColor) * texColor.a; }
          else if( blendMode==2 ) { factor *= hardness; 
            resultfinal = vec4( 1.-((1.-(texColor.rgb/texColor.a))*(1.-factor.rgb)),1.0) * texColor.a; }
          // else if( blendMode==3 ) { factor *= hardness; 
            // resultfinal = (newLayer(texColor, factor) * hardness) * texColor.a; }
              // resultfinal = max(factor, texColor) * texColor.a; }
            
    	gl_FragColor = mix(texColor, resultfinal, alpha) * texColor.a;
          }
        }

    </shader>

</effect>