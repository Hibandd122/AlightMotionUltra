<?xml version='1.0' encoding='utf-8'?>
<effect category_ultra="Blur, Focus & Sharpen" id="com.alightcreative.effects.bubble" name="Bubble" desc="Draws animated procedural soap bubbles using jittered cell noise, with six toggleable size classes from massive to tiny; some classes use a supersampled blur. Composited over the layer in normal or screen mode with transform controls. Enabling all classes is expensive." category="blur" tags="other" thumb="thumb/9/bubble.png" experimental="false">

    <params>
        <switch id="autop" default="true" label="Auto Progress" />
        <slider id="PROGRESS" label="Progress" default="1.0" min="-100.0" max="100.0" step="0.01" type="percent" />
        <slider id="SPEED" label="Speed" default="1.0" min="0.0" max="100.0" step="0.01" type="percent" />
        
        <switch id="B1" default="true" label="Massive Bubble" />
        <switch id="B2" default="false" label="Huge Bubble" />
        <switch id="B3" default="true" label="Big Bubble" />
        <switch id="B4" default="false" label="Medium Bubble" />
        <switch id="B5" default="false" label="Small Bubble" />
        <switch id="B6" default="false" label="Tiny Bubble" />
        
        <slider id="scale" default="0.75" min="0.1" max="50.0" step="0.01" label="Scale" type="percent" />
        <slider id="angle" default="0.0" min="-3600" max="3600" ticks="-180,-135,-90,-45,-30,-15,0,15,30,45,90,135,180" label="Rotate" step="0.5" type="angle" />
        <point id="position" label="@am:string/effect_param_offset" />
        <point id="stretch" default="1.0,1.0" min="0.01,0.01" max="10, 10" label="@am:string/effect_param_stretch" step="0.01" />
        <slider id="alpha" default="1.0" min="0.0" max="1.0" step="0.01" label="Alpha" type="percent" />
        <slider id="hardness" default="1.0" min="1.0" max="3.0" step="0.01" label="Hard/Brightness" type="percent" />
        <menu id="blendMode" style="radio" label="Blending Mode" default="1">
            <menu label="NORMAL" value="0" />
            <menu label="SCREEN" value="1" />
        </menu>
    <texture id="inputImg" srcType="content" />
    <texture id="xcipher" srcType="buffer" downsample="2" />
    </params>
    
    <passes>
        <pass target="xcipher" />
        <pass />
    </passes>
    
    

    <shader type="fragment">
    //////////////////////////
    vec4 newLayer( vec4 a, vec4 b) { return a * (1. - b.a) + b;  }
    mat2 rot2m(float a){ float s = sin(a); float c = cos(a); return mat2(c, -s, s, c); }
    vec2 l2s( vec2 v ) { return (vec4(v,0.,1.)*acLayerToScreen).xy; }
    #define fragCoord gl_FragCoord.xy
    
   /////////////////////////
   
        vec2 uv;
        
        vec2 hash2a(vec2 x,float anim)
        {
        	float r = 523.0*sin(dot(x, vec2(53.3158, 43.6143)));
            float xa1=fract(anim);
            float xb1=anim-xa1;
            anim+=0.5;
            float xa2=fract(anim);
            float xb2=anim-xa2;
            
            vec2 z1=vec2(fract(15.32354 * (r+xb1)), fract(17.25865 * (r+xb1)));
            r=r+1.0;
            vec2 z2=vec2(fract(15.32354 * (r+xb1)), fract(17.25865 * (r+xb1)));
            r=r+1.0;
            vec2 z3=vec2(fract(15.32354 * (r+xb2)), fract(17.25865 * (r+xb2)));
            r=r+1.0;
            vec2 z4=vec2(fract(15.32354 * (r+xb2)), fract(17.25865 * (r+xb2)));
        	return (mix(z1,z2,xa1)+mix(z3,z4,xa2))*0.5;
        }
        
        float hashNull(vec2 x)
        {
            float r = fract(523.0*sin(dot(x, vec2(53.3158, 43.6143))));
            return r;
        }
        
        vec4 NC0=vec4(0.0,157.0,113.0,270.0);
        vec4 NC1=vec4(1.0,158.0,114.0,271.0);
        
        vec4 hash4( vec4 n ) { return fract(sin(n)*753.5453123); }
        vec2 hash2( vec2 n ) { return fract(sin(n)*753.5453123); }
        float noise2( vec2 x )
        {
            vec2 p = floor(x);
            vec2 f = fract(x);
            f = f*f*(3.0-2.0*f);
        	
            float n = p.x + p.y*157.0;
            vec2 s1=mix(hash2(vec2(n)+NC0.xy),hash2(vec2(n)+NC1.xy),vec2(f.x));
            return mix(s1.x,s1.y,f.y);
        }
        
        float noise3( vec3 x )
        {
            vec3 p = floor(x);
            vec3 f = fract(x);
            f = f*f*(3.0-2.0*f);
        	
            float n = p.x + dot(p.yz,vec2(157.0,113.0));
            vec4 s1=mix(hash4(vec4(n)+NC0),hash4(vec4(n)+NC1),vec4(f.x));
            return mix(mix(s1.x,s1.y,f.y),mix(s1.z,s1.w,f.y),f.z);
        }
        
        vec4 booble(vec2 te,vec2 pos,float numCells)
        {
            float d=dot(te, te);
        
            vec2 te1=te+(pos-vec2(0.5,0.5))*0.4/numCells;
            vec2 te2=-te1;
            float zb1=max(pow(noise2(te2*1000.11*d),10.0),0.01);
            float zb2=noise2(te1*1000.11*d);
            float zb3=noise2(te1*200.11*d);
            float zb4=noise2(te1*200.11*d+vec2(20.0));
            
            vec4 colorb=vec4(1.0);
            colorb.xyz=colorb.xyz*(0.7+noise2(te1*1000.11*d)*0.3);
            
            zb2=max(pow(zb2,20.1),0.01);
            colorb.xyz=colorb.xyz*(zb2*1.9);
            
            vec4 color=vec4(noise2(te2*10.8),noise2(te2*9.5+vec2(15.0,15.0)),noise2(te2*11.2+vec2(12.0,12.0)),1.0);
            color=mix(color,vec4(1.0),noise2(te2*20.5+vec2(200.0,200.0)));
            color.xyz=color.xyz*(0.7+noise2(te2*1000.11*d)*0.3);
            color.xyz=color.xyz*(0.2+zb1*1.9);
            
            float r1=max(min((0.033-min(0.04,d))*100.0/sqrt(numCells),1.0),-1.6);
            float d2=(0.06-min(0.06,d))*10.0;
            d=(0.04-min(0.04,d))*10.0;
        	color.xyz=color.xyz+colorb.xyz*d*1.5;
            
            float f1=min(d*10.0,0.5-d)*2.2;
            f1=pow(f1,4.0);
            float f2=min(min(d*4.1,0.9-d)*2.0*r1,1.0);
        
            float f3=min(d2*2.0,0.7-d2)*2.2;
            f3=pow(f3,4.0);
            
        	return vec4(color*max(min(f1+f2,1.0),-0.5)+vec4(zb3)*f3-vec4(zb4)*(f2*0.5+f1)*0.5);
        }
        
        vec4 Cells(vec2 p,vec2 move, in float numCells,in float count,float blur)
        {
            vec2 inp=p+move;
        	inp *= numCells;
        	float d = 1.0;
            vec2 te;
            vec2 pos;
        	for (int xo = -1; xo &lt;= 1; xo++)
        	{
        		for (int yo = -1; yo &lt;= 1; yo++)
        		{
        			vec2 tp = floor(inp) + vec2(xo, yo);
                    vec2 rr=mod(tp, numCells);
                    float iTimee = autop? PROGRESS + (acTime*SPEED) : PROGRESS;
                    tp=tp + (hash2a(rr,iTimee*0.1)+hash2a(rr,iTimee*0.1+0.25))*0.5;
        			vec2 l = inp - tp;
                    float dr=dot(l, l);
                    if (hashNull(rr)&gt;count)
                        if (d&gt;dr) {
        					d = dr;
                            pos=tp;
                        }
        		}
        	}
            if (d&gt;=0.06) return vec4(0.0);
            te=inp-pos;
            
             
            if (d&lt;0.04) uv=uv+te*(d)*2.0;
            if (blur&gt;0.0001) {
                vec4 c=vec4(0.0);
                for (float x=-1.0;x&lt;1.0;x+=0.5) {
                    for (float y=-1.0;y&lt;1.0;y+=0.5) {
                        c+=booble(te+vec2(x,y)*blur,p,numCells);
                    }
                }
                return c*0.05;
            }
        
            return booble(te,p,numCells);
        }
    //////////////////////////
        void main() {
        if( acPass == 0 ) { gl_FragColor = texture2DCv(inputImg.texture, acScreenNorm.st); } else if( acPass==1 ) {
        vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
        vec4 texColorX = vec4(0.0,0.0,0.0,1.0) * texColor.a;
        
        vec2 image_size = getTexSize(inputImg.size);
        vec2 iResolution = vec2(acScreenSize.x, acScreenSize.y);
        vec2 iMouse = vec2(0.5, 0.5);
        
        vec2 umefx;
        umefx = ((vec2(acScreenNorm.x,1.0-acScreenNorm.y)*acScreenSize)-acLayerCenter)/acLayerSize;
        umefx.y*=acLayerSize.y/acLayerSize.x;
        umefx /= scale;
        umefx *= rot2m((0.0+angle)*0.0174533);
        umefx += vec2((0.0+position.x), (0.0+position.y))*vec2(-1.,1.)/500.;
        vec2 scaleVec;
        scaleVec = vec2(scale*stretch.x,scale*stretch.y);
        umefx /= scaleVec;
        
        /////////////////////////
        
    	vec4 fragColor;
    	
            uv = umefx;
            float iTime = autop? PROGRESS + (acTime*SPEED) : PROGRESS;
            
            vec2 l1=vec2(iTime*0.02,iTime*0.02);
            vec2 l2=vec2(-iTime*0.01,iTime*0.007);
            vec2 l3=vec2(0.0,iTime*0.01);
            
            vec4 e= vec4(0.0);
            
            vec4 cr1; vec4 cr2; vec4 cr3; vec4 cr4; vec4 cr5; vec4 cr6;
            
            if(B1) {
            cr1=Cells(uv,vec2(20.2449,93.78)+l1,2.0,0.5,0.005); }
            if(B2) {
            cr2=Cells(uv,vec2(0.0,0.0),3.0,0.5,0.003); }
            if(B3) {
            cr3=Cells(uv,vec2(230.79,193.2)+l2,4.0,0.5,0.0); }
            if(B4) {
            cr4=Cells(uv,vec2(200.19,393.2)+l3,7.0,0.8,0.01); }
            if(B5) {
            cr5=Cells(uv,vec2(10.3245,233.645)+l3,9.2,0.9,0.02); }
            if(B6) {
            cr6=Cells(uv,vec2(10.3245,233.645)+l3,14.2,0.95,0.05); }
            
            if(B1) {
            e=max(e-vec4(dot(cr1,cr1))*0.1,0.0)+cr1*1.8; }
            if(B2) {
            e=max(e-vec4(dot(cr2,cr2))*0.1,0.0)+cr2*1.4; }
            if(B3) {
            e=max(e-vec4(dot(cr3,cr3))*0.1,0.0)+cr3*1.1; }
            if(B4) {
            e=max(e-vec4(dot(cr4,cr4))*0.1,0.0)+cr4*1.3; }
            if(B5) {
            e=max(e-vec4(dot(cr5,cr5))*0.1,0.0)+cr5*1.6; }
            if(B6) {
            e=max(e-vec4(dot(cr6,cr6))*0.1,0.0)+cr6*1.6; }
            
            fragColor= e;
        
    	vec4 factor = mix(texColorX,fragColor,1.0);
    	
        vec4 resultfinal;
          if( blendMode==0 ) {
            resultfinal = (factor * hardness) * texColor.a; }
          else if( blendMode==1 ) { factor *= hardness; 
            resultfinal = vec4( 1.-((1.-(texColor.rgb/texColor.a))*(1.-factor.rgb)),1.0) * texColor.a; }
            
    	gl_FragColor = mix(texColor, resultfinal, alpha) * texColor.a;
          }
        }

    </shader>

</effect>