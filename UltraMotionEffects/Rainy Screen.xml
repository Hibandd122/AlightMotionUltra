<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.effects.rainyfalls" name="Rainy Screen" desc="Displaces the image with scrolling noise-texture lookups to imitate rain streaks running down a screen. X/Y Scale set streak size and X/Y Distort the displacement strength, with auto progress and speed controls." category="procedural" tags="other" thumb="thumb/Distort and Warp/Rainy Screen.webp" experimental="false" overdraw="true" max-overdraw="1.0">

    <params>
        <texture id="noisexx" srcType="image" src="textures/noise-rgb-static.png" />
        <switch id="autop" default="true" label="Auto Progress" />
        <slider id="PROGRESS" label="Progress" default="1.0" min="-100.0" max="100.0" step="0.01" type="percent" />
        <slider id="SPEED" label="Speed AutoProg" default="1.0" min="0.0" max="100.0" step="0.01" type="percent" />
        
        <slider id="xscale" label="X Scale" default="0.50" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="yscale" label="Y Scale" default="12.0" min="0.0" max="100.0" step="0.05" type="percent" />
        <slider id="xdistort" label="X Distort" default="0.05" min="0.0" max="100.0" step="0.001" type="percent" />
        <slider id="ydistort" label="Y Distort" default="0.01" min="0.0" max="100.0" step="0.001" type="percent" />
        
        <slider id="alpha" default="1.0" min="0.0" max="1.0" step="0.01" label="Alpha" type="percent" />
    <texture id="inputImg" srcType="content" />
    <texture id="xcipher" srcType="buffer" downsample="2" />
    </params>
    
    <passes>
        <pass target="xcipher" />
        <pass />
    </passes>
    
    

    <shader type="fragment">
    
    vec4 newLayer( vec4 a, vec4 b) { return a * (1. - b.a) + b;  }
    mat2 rot2m(float a){ float s = sin(a); float c = cos(a); return mat2(c, -s, s, c); }
    vec2 l2s( vec2 v ) { return (vec4(v,0.,1.)*acLayerToScreen).xy; }
    #define fragCoord gl_FragCoord.xy
    
    //////////////////////////
        
    //////////////////////////
        void main() {
        if( acPass == 0 ) { gl_FragColor = texture2DCv(inputImg.texture, acScreenNorm.st); } else if( acPass==1 ) {
        float speed  = 0.08;
            
        vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
        vec4 texColorX = vec4(0.0,0.0,0.0,1.0) * texColor.a;
        
        vec2 image_size = getTexSize(inputImg.size);
        vec2 iResolution = vec2(acScreenSize.x, acScreenSize.y);
        vec2 iMouse = vec2(0.5, 0.5);
        float iTime = autop? PROGRESS + (acTime*SPEED) : PROGRESS;
        
        vec2 umefx;
        umefx = acScreenNorm; 
        /////////////////////////
        
    	vec4 fragColor;
    	
        vec2 uv = umefx;
        vec2 uv_warp = vec2(uv.x/xscale,uv.y/yscale + iTime*speed);
    	vec2 uv_samp = vec2(texture2DCv(noisexx.texture,uv_warp).r,texture2DCv(noisexx.texture,vec2(uv_warp.x,uv_warp.y + speed)).r);
    	fragColor = texture2DCv(inputImg.texture,vec2(uv.x+(uv_samp.x-uv_samp.y)*xdistort,uv.y + uv_samp.y*ydistort));
        
    	gl_FragColor = mix(texColor, fragColor, alpha) * texColor.a;
          }
        }

    </shader>

</effect>