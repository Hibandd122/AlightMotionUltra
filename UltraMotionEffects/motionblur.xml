<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.effects.motionblur" name="@am:string/effect_mblur_name" desc="@am:string/effect_mblur_desc" category="blur" tags="blur,auto,essentials,raster" thumb="thumb/mblur.webp" deprecated="true">

    
        
            
            
            
            
        
    

    <params>
        <texture id="inputImg" srcType="content" />
        
        <slider id="tune" default="1.0" min="0.0" max="4.0" label="@am:string/effect_param_tune" step="0.1" snap="1.0" ticks="1.0" />
        <switch id="usePos" default="true" label="@am:string/effect_param_position" />
        <switch id="useScale" default="true" label="@am:string/effect_param_scale" />
        <switch id="useAngle" default="true" label="@am:string/effect_param_angle" />
    </params>

    


    <shader type="fragment" group="0" precision="high">

        void main() {
            float angle = abs(acAngularVelocity*tune);
            float cx = 0.5 + acLayerPivot.x/acLayerSize.x;
            float cy = 0.5 - acLayerPivot.y/acLayerSize.y;
            vec2 image_size = getTexSize(inputImg.size);
            vec2 texelSize = 1.0 / image_size;
            vec2 center = (vec4(cx,cy,0.0,1.0) * acLayerToScreen).xy;
            vec2 v = acScreenNorm.st - center;
            v.y *= image_size.y / image_size.x;
            float r = length(v);
            float d = 6.283185*r;
            float l = d*angle/360.0;
            float a = angle * 0.0174533;
            float amin = a/2.0;
            float speed = l/texelSize.x;
            int nSamples = int(clamp(speed, 1.01, 100.01));
            vec4 outColor = texture2DCv(inputImg.texture,acScreenNorm.st);
            for(int i=1; i&lt;nSamples; i++ ) {
                float sampleAngle = -amin + a * float(i)/float(nSamples);
                float s = sin(sampleAngle);
                float c = cos(sampleAngle);
                mat2 rot = mat2(c, -s, s, c);
                vec2 temp = v*rot;
                temp.y = temp.y * image_size.x / image_size.y;
                outColor += texture2DCv(inputImg.texture, center + temp);
            }
            gl_FragColor = outColor/float(nSamples);
        }

    </shader>

    <shader type="fragment" group="1" precision="high">

        void main() {
            vec2 image_size = getTexSize(inputImg.size);
            vec2 texelSize = 1.0 / image_size;
            float cx = 0.5 + acLayerPivot.x/acLayerSize.x;
            float cy = 0.5 - acLayerPivot.y/acLayerSize.y;
            vec2 center = vec2(cx,cy);
            float strength = acScaleVelocity*tune/length(acLayerSize);
            vec2 v = acScreenNorm.st - (vec4(center,0.0,1.0) * acLayerToScreen).xy;
            v.y *= image_size.y / image_size.x;
            float speed = length(strength / 2.0 / texelSize);
            speed *= length( acLayerNorm.st - center ); // Adaptive
            int nSamples = int(clamp(speed, 1.01, 100.01));
            vec4 outColor = texture2DCv(inputImg.texture,acScreenNorm.st);
            vec2 vnorm = normalize(v)*length(texelSize)*speed;
            for(int i=1; i&lt;nSamples; i++ ) {
                vec2 offset = vnorm * (float(i) / float(nSamples - 1) - 0.5);
                offset.y = offset.y * image_size.x / image_size.y;
                outColor += texture2DCv(inputImg.texture, acScreenNorm.st-offset);
            }
            gl_FragColor = outColor/float(nSamples);
        }

    </shader>

    <shader type="fragment" group="2" precision="high">

        void main() {
            vec2 texelSize = 1.0 / getTexSize(inputImg.size);
            vec2 velocity = acVelocity*tune / acScreenSize;
            float speed = length(velocity / texelSize);
            int nSamples = int(clamp(speed, 1.01, 100.01));
            vec4 outColor = texture2DCv(inputImg.texture,acScreenNorm.st);
            for(int i=1; i&lt;nSamples; i++ ) {
                vec2 offset = velocity * (float(i) / float(nSamples - 1) - 0.5);
                outColor += texture2DCv(inputImg.texture, acScreenNorm.st-offset);
            }
            gl_FragColor = outColor/float(nSamples);
        }

    </shader>

</effect>