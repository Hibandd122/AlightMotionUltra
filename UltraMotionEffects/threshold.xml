<?xml version='1.0' encoding='utf-8'?>
<effect category_ultra="Glitch & Digital Damage" id="com.alightcreative.effects.threshold" name="@am:string/effect_threshold_name" desc="@am:string/effect_threshold_desc" category="distort" tags="color,glitch,raster" thumb="thumb/threshold.webp">

    <params>
        <texture id="inputImg" srcType="content" />
        <slider id="threshold" default="0.4" min="0.0" max="1.0" label="@am:string/effect_param_threshold" />
        <slider id="feather" default="0.0" min="0.0" max="1.0" label="@am:string/effect_param_feather" />
        <switch id="invert" default="false" label="@am:string/effect_param_invert" />
        <menu id="blendMode" default="0" style="dropdown" label="@am:string/effect_param_blendmode">
            <menu label="@am:string/effect_param_normal" value="0" />
            <menu label="@am:string/effect_param_multiply" value="1" />
            <menu label="@am:string/effect_param_screen" value="2" />
        </menu>
    </params>

    <shader type="fragment">

        const vec3 luminanceWeighting = vec3(0.2126, 0.7152, 0.0722);

        void main() {
            vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
            float luminance = dot(texColor.rgb, luminanceWeighting);
            float f = max(min((feather/4.),abs(0.5-threshold)),0.00196);
            float t = ((threshold-0.5)*(1.003+f*2.))+0.5;
            float p = smoothstep(t-f, t+f, luminance);

            if( invert )
                p = 1.-p;

            vec3 rgb;
            if( blendMode==1 ) {
                rgb = texColor.rgb * p;
            } else if( blendMode==2 ) {
                rgb = 1.-((1.-texColor.rgb)*(1.-p));
            } else {
                rgb = vec3(p,p,p);
            }

            gl_FragColor = vec4(rgb,texColor.a);
        }

    </shader>

</effect>