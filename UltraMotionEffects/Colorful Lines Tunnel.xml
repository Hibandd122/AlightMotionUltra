<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.effects.colortunnel2" name="Colorful Lines Tunnel" desc="Flies a camera through up to 16 stacked planes of glowing polar ring segments, alpha-blended front to back to form an animated colored tunnel. Controls for plane count (Furthest), oversampling, glow factor, line warping, cutoff, plane distance and RGB color mix." category="procedural" tags="other" thumb="thumb/Procedural/Colorful Lines Tunnel.webp" experimental="false">

    <params>
        
        <switch id="autop" default="true" label="Auto Progress" />
        <slider id="PROGRESS" label="Progress" default="1.0" min="-100.0" max="100.0" step="0.01" type="percent" />
        <slider id="SPEED" label="Speed AutoProg" default="1.0" min="0.0" max="100.0" step="0.01" type="percent" />
        
        
        <slider id="FURT" default="16" min="1" max="18" label="Furthest" type="integer" step="1" />
        <slider id="overSample" label="OverSample" default="4.0" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="glowFactor" label="Glow Factor" default="0.05" min="0.0" max="100.0" step="0.001" type="percent" />
        <slider id="rdd_per" label="Warping Line" default="10" min="0.0" max="100.0" step="0.1" type="percent" />
        <slider id="cutOff" label="CutOff" default="0.95" min="0.0" max="1.0" step="0.01" type="percent" />
        <slider id="planeDist" label="Plane Dist" default="0.2" min="0.0" max="100.0" step="0.01" type="percent" />
        
        <slider id="MIXR" label="RED Mix" default="2" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="MIXG" label="Green Mix" default="3" min="0.0" max="100.0" step="0.01" type="percent" />
        <slider id="MIXB" label="Blue Mix" default="1" min="0.0" max="100.0" step="0.01" type="percent" />
        
        <slider id="scale" default="1.0" min="0.1" max="50.0" step="0.01" label="Scale" type="percent" />
        <slider id="angle" default="0.0" min="-3600" max="3600" ticks="-180,-135,-90,-45,-30,-15,0,15,30,45,90,135,180" label="Rotate" step="0.5" type="angle" />
        <point id="position" label="@am:string/effect_param_offset" />
        <point id="stretch" default="1.0,1.0" min="0.01,0.01" max="10, 10" label="@am:string/effect_param_stretch" step="0.01" />
        <slider id="alpha" default="1.0" min="0.0" max="1.0" step="0.01" label="Alpha" type="percent" />
        <slider id="hardness" default="1.0" min="1.0" max="3.0" step="0.01" label="Hard/Brightness" type="percent" />
        <menu id="blendMode" style="radio" default="3">
            <menu label="NORMAL" value="0" />
            <menu label="LAYER" value="3" />
            <menu label="MULTIPLY" value="1" />
            <menu label="SCREEN" value="2" />
        </menu>
        
        
        
        <texture id="inputImg" srcType="content" />
        <texture id="buff1" srcType="buffer" />

    </params>
    
    <passes><pass target="buff1" /><pass /></passes>
    
    <shader type="fragment" precision="high">
    
    vec4 newLayer( vec4 a, vec4 b) { return a * (1. - b.a) + b;  }
    mat2 rot2m(float a){ float s = sin(a); float c = cos(a); return mat2(c, -s, s, c); }
    vec2 l2s( vec2 v ) { return (vec4(v,0.,1.)*acLayerToScreen).xy; }
    #define fragCoord gl_FragCoord.xy
    
   /////////////////////////
   
        // #define RESOLUTION  iResolution
        // #define TIME        iTime
        #define PI          3.141592654
        #define PI_2        (0.5*PI)
        #define TAU         (2.0*PI)
        #define ROT(a)      mat2(cos(a), sin(a), -sin(a), cos(a))
        #define BPM         (157.0/4.0)
        #define PCOS(a)     0.5*(cos(a)+1.0)
        
        // const float planeDist = 1.0-0.80;
        // const int   furthest  = 16;
        // const int   fadeFrom  = max(furthest-4, 0);
        // const float fadeDist  = planeDist*float(furthest - fadeFrom);
        
        // const float overSample  = 4.0;
        // const float ringDistance= 0.075*overSample/4.0;
        // const float noOfRings   = 20.0*4.0/overSample;
        // const float glowFactor  = 0.05;
        
        
        // License: Unknown, author: Unknown, found: don't remember
        vec4 alphaBlend(vec4 back, vec4 front) {
          float w = front.w + back.w*(1.0-front.w);
          vec3 xyz = (front.xyz*front.w + back.xyz*back.w*(1.0-front.w))/w;
          return w &gt; 0.0 ? vec4(xyz, w) : vec4(0.0);
        }
        
        // License: Unknown, author: Unknown, found: don't remember
        vec3 alphaBlend(vec3 back, vec4 front) {
          return mix(back, front.xyz, front.w);
        }
        
        // License: Unknown, author: Unknown, found: don't remember
        float tanh_approx(float x) {
          //  Found this somewhere on the interwebs
          //  return tanh(x);
          float x2 = x*x;
          return clamp(x*(27.0 + x2)/(27.0+9.0*x2), -1.0, 1.0);
        }
        
        // License: Unknown, author: Unknown, found: don't remember
        float hash(float co) {
          return fract(sin(co*12.9898) * 13758.5453);
        }
        
        vec3 offset(float z) {
          float a = z;
          vec2 p = -0.15*(vec2(cos(a), sin(a*sqrt(2.0))) + vec2(cos(a*sqrt(0.75)), sin(a*sqrt(0.5))));
          return vec3(p, z);
        }
        
        vec3 doffset(float z) {
          float eps = 0.05;
          return 0.5*(offset(z + eps) - offset(z - eps))/(2.0*eps);
        }
        
        vec3 ddoffset(float z) {
          float eps = 0.05;
          return 0.5*(doffset(z + eps) - doffset(z - eps))/(2.0*eps);
        }
        
        vec3 skyColor(vec3 ro, vec3 rd) {
          return vec3(0.0);
        }
        
        // License: MIT OR CC-BY-NC-4.0, author: mercury, found: https://mercury.sexy/hg_sdf/
        float mod1(inout float p, float size) {
          float halfsize = size*0.5;
          float c = floor((p + halfsize)/size);
          p = mod(p + halfsize, size) - halfsize;
          return c;
        }
        
        // License: MIT, author: Pascal Gilcher, found: https://www.shadertoy.com/view/flSXRV
        float atan_approx(float y, float x) {
          float cosatan2 = x / (abs(x) + abs(y));
          float t = PI_2 - cosatan2 * PI_2;
          return y &lt; 0.0 ? -t : t;
        }
        
        
        // License: CC0, author: Mårten Rånge, found: https://github.com/mrange/glsl-snippets
        vec2 toPolar(vec2 p) {
          return vec2(length(p), atan_approx(p.y, p.x));
        }
        
        vec3 glow(vec2 pp, float h) {
        
          float ringDistance= 0.075*overSample/4.0;
          // float glowFactor  = 0.05;
        
          float hh = fract(h*8677.0);
          float iTime = autop? PROGRESS + (acTime*SPEED) : PROGRESS;
          float b = TAU*h+0.5*iTime*(hh &gt; 0.5 ? 1.0 : -1.0);
          float a = pp.y+b;
          float d = max(abs(pp.x)-0.001, 0.00125);
          return 
            (   smoothstep(0.667*ringDistance, 0.2*ringDistance, d)
              * smoothstep(0.1, 1.0, cos(a))
              * glowFactor
              * ringDistance
              / d
            )
            * (cos(a+b+vec3(0,1,2))+vec3(1.0))
            ;
        }
        
        vec3 glowRings(vec2 p, float hh) {
          vec2 pp = toPolar(p);
        
        //  pp.y += TAU*hh;
          vec3 col = vec3(0.0);
          float h = 1.0;
          
          float ringDistance= 0.075*overSample/4.0;
          float noOfRings   = 20.0*4.0/overSample;
        
          for (float i = 0.0; i &lt; overSample; ++i) {
            vec2 ipp = pp;
            float nr = 1.0/overSample;
            ipp.x -= ringDistance*(nr*i);
            float rn = mod1(ipp.x, ringDistance); 
            h = hash(rn+123.0*i);
            col += glow(ipp, h)*step(rn, noOfRings);
          }
          
          col += (0.01*vec3(1.0, 0.25, 0.0))/length(p);
        
          return col;
        }
        
        vec4 plane(vec3 ro, vec3 rd, vec3 pp, vec3 off, float aa, float n) {
          float h = hash(n+123.4);
        
          vec3 hn;
          vec2 p = (pp-off*vec3(1.0, 1.0, 0.0)).xy;
          float l = length(p);
          float fade = smoothstep(0.1, 0.15, l);
          if (fade &lt; 0.1) return vec4(0.0);
          vec4 col = vec4(0.0);
          
          col.xyz = glowRings(p*mix(0.5, 4.0, h), h);
          float i = max(max(col.x, col.y), col.z);
        
          col.w = (tanh_approx(0.5+max((i), 0.0))*fade);
          return col;
        }
        
        vec3 color(vec3 ww, vec3 uu, vec3 vv, vec3 ro, vec2 p) {
          float lp = length(p);
          vec2 np = p + 1.0/acScreenNorm.st;
          
          // const float rdd_per   = 10.0;
          
          float rdd =  (1.75+0.75*pow(lp,1.5)*tanh_approx(lp+0.9*PCOS(rdd_per*p.x)*PCOS(rdd_per*p.y)));
        //  float rdd = 2.0;
          
          vec3 rd = normalize(p.x*uu + p.y*vv + rdd*ww);
          vec3 nrd = normalize(np.x*uu + np.y*vv + rdd*ww);
        
          float nz = floor(ro.z / planeDist);
        
          vec3 skyCol = skyColor(ro, rd);
        
        
          vec4 acol = vec4(0.0);
          
          // const float cutOff = 0.95;
          
          bool cutOut = false;
        
          float maxpd = 0.0;
        
          // Steps from nearest to furthest plane and accumulates the color 
          for (int i = 1; i &lt;= 16; ++i) {
          // for (int i = 1; i &lt;= furthest; ++i) {
            float pz = planeDist*nz + planeDist*float(i);
        
            float pd = (pz - ro.z)/rd.z;
        
            if (pd &gt; 0.0 &amp;&amp; acol.w &lt; cutOff) {
              vec3 pp = ro + rd*pd;
              maxpd = pd;
              vec3 npp = ro + nrd*pd;
        
              float aa = 3.0*length(pp - npp);
        
              vec3 off = offset(pp.z);
        
              vec4 pcol = plane(ro, rd, pp, off, aa, nz+float(i));
        
        int furthest  = int(FURT);
        int   fadeFrom  = max(furthest-4, 0);
        float fadeDist  = planeDist*float(furthest - fadeFrom);
        
              float nz = pp.z-ro.z;
              float fadeIn = smoothstep(planeDist*float(furthest), planeDist*float(fadeFrom), nz);
              float fadeOut = smoothstep(0.0, planeDist*0.1, nz);
              pcol.w *= fadeOut*fadeIn;
              pcol = clamp(pcol, 0.0, 1.0);
        
              acol = alphaBlend(pcol, acol);
            } else {
              cutOut = true;
              acol.w = acol.w &gt; cutOff ? 1.0 : acol.w;
              break;
            }
        
          }
        
          vec3 col = alphaBlend(skyCol, acol);
        // To debug cutouts due to transparency  
        //  col += cutOut ? vec3(1.0, -1.0, 0.0) : vec3(0.0);
          return col;
        }
        
        vec3 effect(vec2 p, vec2 q) {
        float iTime = autop? PROGRESS + (acTime*SPEED) : PROGRESS;
          float tm  = planeDist*iTime*BPM/60.0;
          vec3 ro   = offset(tm);
          vec3 dro  = doffset(tm);
          vec3 ddro = ddoffset(tm);
        
          vec3 ww = normalize(dro);
          vec3 uu = normalize(cross(normalize(vec3(0.0,1.0,0.0)+ddro), ww));
          vec3 vv = cross(ww, uu);
        
          vec3 col = color(ww, uu, vv, ro, p);
          
          // Random color tweaks
          col -= 0.075*vec3(MIXR, MIXG, MIXB);
          col *= sqrt(2.0);
          col = clamp(col, 0.0, 1.0);
          col = sqrt(col);
          return col;
        }
   
   /////////////////////////
    void main ()
    {
            
        vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
        vec4 texColorX = vec4(0.0,0.0,0.0,1.0) * texColor.a;
        
        vec2 image_size = getTexSize(inputImg.size);
        vec2 iResolution = vec2(acScreenSize.x, acScreenSize.y);
        vec2 iMouse = vec2(0.5, 0.5);
        // float iTime = autop? PROGRESS + (acTime*SPEED) : PROGRESS;
        
        vec2 umefx;
        // umefx = acScreenNorm; 
        umefx = ((vec2(acScreenNorm.x,1.0-acScreenNorm.y)*acScreenSize)-acLayerCenter)/acLayerSize;
        umefx.y*=acLayerSize.y/acLayerSize.x;
        
        umefx /= scale;
        umefx *= rot2m((0.0+angle)*0.0174533);
        umefx += vec2((-70.0+position.x), (150.0+position.y))*vec2(-1.,1.)/500.;
        vec2 scaleVec;
        scaleVec = vec2(scale*stretch.x,scale*stretch.y);
        umefx /= scaleVec;
        
        /////////////////////////
        
    	vec4 fragColor;
    	
          vec2 q = umefx;
          vec3 col = effect(q, q);
          
          fragColor = vec4(col, 1.0);
        
    	    vec4 factor = mix(texColorX,fragColor,1.0);

        vec4 resultfinal;
          if( blendMode==0 ) { resultfinal = (factor * hardness) * texColor.a; }
          else if( blendMode==1 ) { factor *= hardness;
            vec4 c = factor + vec4(1.)*(1.-factor.a);
            resultfinal = vec4((texColor.rgb/texColor.a)*c.rgb,1.0) * texColor.a; }
            // resultfinal = min(factor, texColor) * texColor.a; }
          else if( blendMode==2 ) { factor *= hardness; 
            resultfinal = vec4( 1.-((1.-(texColor.rgb/texColor.a))*(1.-factor.rgb)),1.0) * texColor.a; }
              // resultfinal = max(factor, texColor) * texColor.a; }
          else if( blendMode==3 ) { factor *= hardness; resultfinal = (newLayer(texColor, factor) * hardness) * texColor.a; }
            
    	gl_FragColor = mix(texColor, resultfinal, alpha) * texColor.a;
    	// }
    }


    </shader>

</effect>