<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.effects.instantgrass" name="2D Grass" desc="Renders animated procedural grass-blade silhouettes on stacked depth planes, recolored via luminance mapping between a background and grass color. Amount sets the plane count; Progress and Speed drive the sway, with scale, rotate, offset and blend-mode controls." category="procedural" tags="other" thumb="thumb/Procedural/2D Grass.webp" experimental="false">

    <params>
        <slider id="AMOUNT" label="Amount" default="10" min="0" max="20" step="1.0" type="integer" />
        <switch id="autop" default="true" label="Auto Progress" />
        <slider id="PROGRESS" label="Progress" default="1.0" min="-100.0" max="100.0" step="0.01" type="percent" />
        <slider id="SPEED" label="Speed AutoProg" default="1.0" min="0.0" max="100.0" step="0.01" type="percent" />
        
        <slider id="scale" default="1.0" min="0.1" max="50.0" step="0.01" label="Scale" type="percent" />
        <slider id="angle" default="0.0" min="-3600" max="3600" ticks="-180,-135,-90,-45,-30,-15,0,15,30,45,90,135,180" label="Rotate" step="0.5" type="angle" />
        <point id="position" label="@am:string/effect_param_offset" />
        <point id="stretch" default="1.0,1.0" min="0.01,0.01" max="10, 10" label="@am:string/effect_param_stretch" step="0.01" />
        <slider id="alpha" default="1.0" min="0.0" max="1.0" step="0.01" label="Alpha" type="percent" />
        <slider id="hardness" default="1.2" min="0.0" max="3.0" step="0.01" label="Hardness" type="percent" />
        <color id="color1" default="#00000000" label="Color BG" alpha="true" />
        <color id="color2" default="#FF1ED91E" label="Grass Color" alpha="true" />
        
        <menu id="blendMode" style="radio" default="3">
            <menu label="NORMAL" value="0" />
            <menu label="LAYER" value="3" />
            <menu label="MULTIPLY" value="1" />
            <menu label="SCREEN" value="2" />
        </menu>
    <texture id="inputImg" srcType="content" />
    <texture id="xcipher" srcType="buffer" downsample="2" />
    </params>
    
    <passes>
        <pass target="xcipher" />
        <pass />
    </passes>
    
    

    <shader type="fragment">
    
    vec4 newLayer( vec4 a, vec4 b) { return a * (1. - b.a) + b;  }
    mat2 rot2m(float a){ float s = sin(a); float c = cos(a); return mat2(c, -s, s, c); }
    vec2 l2s( vec2 v ) { return (vec4(v,0.,1.)*acLayerToScreen).xy; }
    #define fragCoord gl_FragCoord.xy
    
    //////////////////////////
        vec4 grass(vec2 p, float x)
        {
                float iTime = autop? PROGRESS + (acTime*SPEED) : PROGRESS;
        	float s = mix(0.9, 1.4, 0.5 + sin(x * 11.0) * 0.2);
        	p.x += pow(1.0 + p.y, 2.0) * 0.1 * cos(x * 0.5 + iTime);
        	p.x *= s;
        	p.y = (1.0 + p.y) * s - 1.0;
        	float m = 1.0 - smoothstep(0.0, clamp(1.0 - p.y * 1.5, 0.1, 0.9) * 1.3 * s, pow(abs(p.x) * 80.0, 2.0) + p.y - 0.1);
        	return vec4(vec3(1, 0.2, 0.9), m * smoothstep(-1.0, -0.9, p.y));
        }
        
        vec3 backg(vec3 ro, vec3 rd)
        {
        	vec3 ground = vec3(0.0, 0.0, 0.0);
        	return ground;
        }
    //////////////////////////
        void main() {
        if( acPass == 0 ) { gl_FragColor = texture2DCv(inputImg.texture, acScreenNorm.st); } else if( acPass==1 ) {
        vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
        vec4 texColorX = vec4(0.0,0.0,0.0,1.0) * texColor.a;
        
        vec2 image_size = getTexSize(inputImg.size);
        vec2 iResolution = vec2(acScreenSize.x, acScreenSize.y);
        vec2 iMouse = vec2(0.5, 0.5);
        
        vec2 umefx;
        umefx = acScreenNorm; 
        // umefx = ((vec2(acScreenNorm.x,1.0-acScreenNorm.y)*acScreenSize)-acLayerCenter)/acLayerSize;
        // umefx.y*=acLayerSize.y/acLayerSize.x;
        
        umefx /= scale;
        umefx *= rot2m((0.0+angle)*0.0174533);
        umefx += vec2((0.0+position.x), (-30.0+position.y))*vec2(-1.,1.)/500.;
        vec2 scaleVec;
        scaleVec = vec2(scale*stretch.x,scale*stretch.y);
        umefx /= scaleVec;
        
        /////////////////////////
        
    	vec4 fragColor;
    	
        	// vec3 ct = xxx * 0.01;
        	// vec3 cp = yyy * 0.01;
        	vec3 ct = vec3(0.0, 2.0, 5.0);
        	vec3 cp = vec3(0.0, 0.6, 0.0);
        	vec3 cw = normalize(cp - ct);
        	vec3 cu = normalize(cross(cw, vec3(0.0, 1.0, 0.0)));
        	vec3 cv = normalize(cross(cu, cw));
        	
        	mat3 rm = mat3(cu, cv, cw);
        	
        	vec2 uv = umefx
        	 * 2.0 - vec2(1.0);
        	vec2 t = uv;
        	
        	
        	vec3 ro = cp, rd = rm * vec3(t, -1.3);
        	
        	vec3 fcol = backg(ro, rd);
        	
        	for(int i = 0; i &lt; int(AMOUNT); i += 1)
        	{
        		float z = -(float(int(AMOUNT) - i) * 0.1 + 1.0);
        		vec4 pln = vec4(0.0, 0.0, -1.0, z);
        		float t = (pln.w - dot(pln.xyz, ro)) / dot(pln.xyz, rd);
        		vec2 tc = ro.xy + rd.xy * t;
        		
        		tc.x += cos(float(i) * 3.0) * 4.0;
        		
        		float cell = floor(tc.x);
        		
        		tc.x = (tc.x - cell) - 0.5;
        		
        		vec4 c = grass(tc, float(i) + cell * 10.0);
        		
        		fcol = mix(fcol, c.rgb, step(0.0, t) * c.w);
        	}
        	
        	fcol = pow(fcol * 1.1, vec3(0.8));
        	
        	
        	fragColor.rgb = fcol + vec3(0);
        	fragColor.a = 1.0;
        
    	    vec4 factor;
    	    vec4 factor2;
    	    const vec3 luminanceWeighting = vec3(0.2126, 0.7152, 0.0722);
            float luminance = dot(fragColor.rgb, luminanceWeighting);
            vec4 gradmap = mix(color1,color2,luminance)*fragColor.a;
            vec4 gradmap2 = mix(vec4(0.0),color2,luminance)*fragColor.a;
    	    // if(GRADMAP) { 
    	    factor = mix(texColorX,gradmap,1.0); // }
    	    factor2 = mix(texColorX,gradmap2,1.0); // }

        vec4 resultfinal;
          if( blendMode==0 ) { resultfinal = (factor * hardness) * texColor.a; }
          else if( blendMode==1 ) { factor *= hardness;
            vec4 c = factor + vec4(1.)*(1.-factor.a);
            resultfinal = vec4((texColor.rgb/texColor.a)*c.rgb,1.0) * texColor.a; }
            // resultfinal = min(factor, texColor) * texColor.a; }
          else if( blendMode==2 ) { factor *= hardness; 
            resultfinal = vec4( 1.-((1.-(texColor.rgb/texColor.a))*(1.-factor.rgb)),1.0) * texColor.a; }
              // resultfinal = max(factor, texColor) * texColor.a; }
          else if( blendMode==3 ) { factor2 *= hardness; resultfinal = (newLayer(texColor, factor2*hardness)) * texColor.a; }
            
    	gl_FragColor = mix(texColor, resultfinal, alpha) * texColor.a;
          }
        }

    </shader>

</effect>