<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.blend.exclusion" name="Blend/Hard Light" internal="true" thumb="thumb/efek.png" category="other">

<params>
    <texture id="inputImg" srcType="content" />
    <texture id="comp" srcType="comp" />
</params>

<shader type="fragment" precision="high">

        void main() {
            // Setup
            vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
            vec4 compColor = texture2DCv(comp.texture,acScreenNorm);
            vec3 top = texColor.rgb;
            vec3 bot = compColor.rgb * texColor.a;

            // Blend top, bot
            vec3 result = 0.5 - 2.*(bot-0.5)*(top-0.5);

            // Output result
            gl_FragColor = vec4(result,texColor.a);
        }

    </shader>

</effect>