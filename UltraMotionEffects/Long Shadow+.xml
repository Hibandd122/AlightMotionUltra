<?xml version='1.0' encoding='utf-8'?>
<effect category_ultra="Glow & Gradients" id="com.alightcreative.effects.longshadow2new" name="Long Shadow+" desc="Casts a long directional shadow from the layer's alpha and fills it with a two-color gradient (linear, radial or sweep). Size and Angle set the cast; Solidity, Fade and Fade Type shape the falloff, with gradient position/scale/rotation controls." category="color" tags="color,pop,raster" thumb="thumb/4/long.png" overdraw="true" max-overdraw="1.5">

    <params>
        <texture id="inputImg" srcType="content" />
        <slider id="size" default="0.05" min="0.0" max="1.0" step="0.01" label="@am:string/effect_param_size" />
        <slider id="angle" default="45.0" min="0.0" max="3600.0" label="@am:string/effect_param_angle" type="angle" step="0.5" ticks="0,45,90,135,180,225,270,315,360" snap="45,90,135" />
        <slider id="alpha" default="0.5" min="0" max="1" step="0.01" label="@am:string/effect_param_alpha" type="percent" />
        <slider id="solidity" default="1" min="0" max="1" step="0.01" label="@am:string/effect_param_solidity" type="percent" />
        <slider id="fill" default="1.0" min="0.0" max="1.0" label="@am:string/effect_param_fill" type="percent" step="0.01" ticks="0.5" />
        <slider id="fade" default="0" min="0" max="1" step="0.01" label="@am:string/effect_param_fade" type="percent" />
        <menu id="fadeType" default="0" style="radio" label="@am:string/effect_param_fadetype">
            <menu label="@am:string/effect_param_exact" value="0" />
            <menu label="@am:string/effect_param_angle" value="1" />
            <menu label="@am:string/effect_param_axis_x" value="2" />
            <menu label="@am:string/effect_param_axis_y" value="3" />
        </menu>
        
        <color id="color1" default="#FF00FFFF" label="Color 1" alpha="true" />
        <color id="color2" default="#FFFF00FF" label="Color 2" alpha="true" />
        <menu id="gradType" style="radio" default="0">
            <menu label="@am:string/effect_param_grad_linear" value="0" />
            <menu label="@am:string/effect_param_grad_radial" value="1" />
            <menu label="@am:string/effect_param_grad_sweep" value="2" />
        </menu>
        <point id="position" label="@am:string/effect_param_offset" />
        <slider id="scale" default="1.0" min="0.1" max="5.0" step="0.01" label="Scale" type="percent" />
        <slider id="angleg" default="0.0" min="-3600" max="3600" ticks="-180,-135,-90,-45,-30,-15,0,15,30,45,90,135,180" label="Rotate" step="0.5" type="angle" />
        
    </params>

    <shader type="fragment">
    
       mat2 rot2m(float a){
        
	float s = sin(a);
	float c = cos(a);
	
	return mat2(c, -s, s, c);
        }

        float projectAndClampDist(vec2 a, vec2 b) {
            return clamp(dot(a,b)/dot(b,b),0.0,1.0);
        }

        void main() {
        
        vec2 umefx;
        umefx = acScreenNorm; 

        umefx /= scale;
        umefx *= rot2m((0.0+angleg)*0.0174533);
        umefx += vec2((0.0+position.x), (0.0+position.y))*vec2(-1.,1.)/500.;

            float t;
            if( gradType == 0 ) {
                t = umefx.x;
            } else if ( gradType == 1 ) {
                t = length( umefx - 0.5 ) * 2.0;
            } else if ( gradType == 2 ) {
                vec2 xy = umefx - 0.5;
                vec2 zw = vec2(0,1.0);
                float a = atan(xy.x, xy.y);
                float b = atan(zw.x, zw.y);
                t = mod(((a-b)+6.28),6.28)/6.28;
            } else {
                t = umefx.x;
            }

            t = clamp(t,0.0,1.0);
            vec4 color = mix(color1, color2, t);
        
            vec2 texelSize = 1.0 / getTexSize(inputImg.size);
            float rad = angle * 0.0174533;
            vec2 velocity = vec2(cos(rad)*size,-sin(rad)*size);
            //velocity *= acPreviewSize*texelSize;
            if( acScreenSize.y &gt; acScreenSize.x ) {
                velocity.y *= acScreenSize.x / acScreenSize.y;
            } else {
                velocity.x *= acScreenSize.y / acScreenSize.x;
            }
            float speed = length(velocity / texelSize);
            int nSamples = int(clamp(speed, 1.01, 500.01));
            float maxAlpha = 0.0;
            float accumAlpha = 0.0;
            int minDist = nSamples;
            vec4 baseColor = texture2DCv(inputImg.texture,acScreenNorm.st);
            for(int i=1; i&lt;nSamples; i++ ) {
                vec2 offset = velocity * (float(i) / float(nSamples - 1));
                vec2 sampleCoord = acScreenNorm.st-offset;
                float insideTexture = step(0.0,sampleCoord.x) * step(sampleCoord.x,1.0) * step(0.0,sampleCoord.y) * step(sampleCoord.y,1.0);
                float sampAlpha = texture2DCv(inputImg.texture, sampleCoord).a * insideTexture;
                maxAlpha = max(maxAlpha, sampAlpha);
                if( sampAlpha &gt; 0.5 ) {
                    minDist = min(minDist,i);
                }
                float adjAlpha = sampAlpha*solidity*0.25;
                accumAlpha = adjAlpha + accumAlpha*(1.0-adjAlpha);
            }
            float resultAlpha = mix(accumAlpha,maxAlpha,solidity);
            float dist = 0.0;
            if( fadeType==0 ) {
                dist = float(minDist)/float(nSamples);
            } else if( fadeType==1 ) {
                dist = projectAndClampDist(acScreenNorm-acLayerCenterNorm,velocity);
            } else if( fadeType==2 ) {
                dist = abs((acScreenNorm-acLayerCenterNorm).x) / abs(velocity.x);
            } else if( fadeType==3 ) {
                dist = abs((acScreenNorm-acLayerCenterNorm).y) / abs(velocity.y);
            }
            //gl_FragColor = mix(mix(color * alpha * (1.0-dist*fade) * resultAlpha, baseColor, baseColor.a * fill),vec4(1.0,0.0,0.0,1.0),abs(sin(dist*25.0)));
            gl_FragColor = mix(color * alpha * (1.0-dist*fade) * resultAlpha, baseColor, baseColor.a * fill);
        }

    </shader>

</effect>