<?xml version='1.0' encoding='utf-8'?>
<effect category_ultra="Color Grading" id="com.alightcreative.effects.satvib" name="@am:string/effect_vibranceSaturation_name" desc="@am:string/effect_vibranceSaturation_desc" category="color" tags="color,raster" thumb="thumb/saturation_vibrance.webp">

    
        
            
            
            
            
            
            
            
            
        
    

    <params>
        <texture id="inputImg" srcType="content" />
        
        
        <slider id="saturation" default="0.0" min="-1.0" max="1.0" label="@am:string/effect_param_saturation" snap="0.0" ticks="0.0" step="0.005" type="relative-percent" />
        <slider id="vib" default="1.0" min="1.0" max="2.0" label="@am:string/effect_param_vibrance" step="0.01" />
        
        
    </params>

    <shader type="fragment">

        void main() {
            //float hue = 0.0;
            highp mat3 rgb2yuv = mat3(
                 0.299,     0.587,    0.114,
                -0.14713,  -0.28886,  0.436,
                 0.615,    -0.51499, -0.10001
            );
            highp mat3 yuv2rgb = mat3(
                1.0,  0.0,      1.13983,
                1.0, -0.39465, -0.58060,
                1.0,  2.03211,  0.0
            );
            //float rad = hue * 0.0174533;
            //highp mat3 huemat = mat3(
            //    1.0,  0.0,       0.0,
            //    0.0,  cos(rad),  sin(rad),
            //    0.0,  -sin(rad), cos(rad)
            //);
            vec4 texColor = texture2DCv(inputImg.texture,acScreenNorm);
            highp vec3 rgb = texColor.rgb;
            lowp float a = texColor.a;
            highp vec3 yuv = rgb * rgb2yuv;

            //-- Vibrance 1 --
            float origSat = min(1.0,length(yuv.yz)*2.5); //
            vec2 satUV = yuv.yz * vib;
            yuv.yz = mix(satUV,yuv.yz,origSat);
            //--------------

            //-- Vibrance 2 --
            //float origSat = min(1.0,length(yuv.yz)*2.5); //
            //vec2 satUV = yuv.yz * pow(origSat+1.0,vib);
            //yuv.yz = mix(satUV,yuv.yz,origSat);
            //--------------

            yuv.yz = yuv.yz * (saturation+1.0);
            //yuv = yuv * huemat;
            //yuv.x = (yuv.x-0.5*a) * (contrast+1.0) + 0.5*a + brightness*a;
            rgb = yuv * yuv2rgb;
            //rgb = (rgb - 0.5) * (contrast+1.0) + 0.5 + brightness;
            gl_FragColor = vec4(rgb,a);
        }

    </shader>

</effect>