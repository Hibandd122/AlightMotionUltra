<?xml version='1.0' encoding='utf-8'?>
<effect id="com.alightcreative.effects.extra2.dither" name="Ordered Dither" desc="Retro ordered-dithering using a Bayer matrix to fake shading with few colors." category="drawingedge" thumb="thumb/Stylize/Ordered Dither.webp" tags="stylize,dither,retro,pixel,raster">

    <params>
        <texture id="inputImg" srcType="content" />
        <slider id="levels" default="3" min="2" max="8" label="Levels" step="1" type="integer" />
    </params>

    <shader type="fragment" precision="high">
        float bayer2(vec2 p){
            return p.x&lt;0.5 ? (p.y&lt;0.5?0.0:3.0) : (p.y&lt;0.5?2.0:1.0);
        }
        void main(){
            vec4 src = texture2DCv(inputImg.texture, acScreenNorm);
            vec3 c = src.a&gt;0.0? src.rgb/src.a : src.rgb;
            vec2 px = floor(acScreenNorm*acScreenSize);
            vec2 coarse = mod(floor(px/2.0), 2.0);
            vec2 fine = mod(px, 2.0);
            float th = (4.0*bayer2(coarse) + bayer2(fine))/16.0;
            float lv = max(levels,2.0);
            vec3 q = floor(c*(lv-1.0) + th)/(lv-1.0);
            gl_FragColor = vec4(clamp(q,0.0,1.0),1.0)*src.a;
        }
    </shader>

</effect>